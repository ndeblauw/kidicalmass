@props(['activity'])

<section class="ride-photo-nudge">
    <p class="ride-photo-nudge__eyebrow">{{ __('components.ride_photo_nudge.eyebrow') }}</p>
    <h2 class="ride-photo-nudge__title">{{ __('components.ride_photo_nudge.heading') }}</h2>
    <p class="ride-photo-nudge__lead">
        {{ __('components.ride_photo_nudge.body') }}
    </p>
</section>
