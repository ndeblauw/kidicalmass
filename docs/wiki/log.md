# Wiki Log

## [2026-06-04] build | Standard hero — unified fixed scroll-over hero + floating pill across interior pages

Frederik wanted the standard interior-page hero reworked toward the MindMarket reference: **eyebrow = page name** (Nunito-regular yellow), an **aspirational title** (big white), the **blue hero behind the menu**, a **compact floating white nav pill**, a **right-side illustration**, and the section below **scrolling over a fixed hero** with rounded top corners. Brainstormed → spec ([`docs/superpowers/specs/2026-06-03-fixed-hero-scroll-over-design.md`](superpowers/specs/2026-06-03-fixed-hero-scroll-over-design.md)) → plan ([`…/plans/2026-06-03-fixed-hero-scroll-over.md`](superpowers/plans/2026-06-03-fixed-hero-scroll-over.md)) → subagent-driven build. **Built:** one shared **`<x-page-hero eyebrow title illustration>`** (anonymous component) with an optional `controls` slot (in-hero) + default slot wrapped in a white `.page-panel`; **pure-CSS scroll-over** — hero `position:fixed` (z-0), spacer holds its place, `.page-panel` (z-10, rounded top) scrolls over it, **floating pill** `.site-header`/`.site-nav` (z-60) always on top. **Migrated & visually verified (desktop+mobile, live Herd):** Kalender (picker kept in hero), Lokale groepen (stat counters in hero), Voor het eerst, Meehelpen, Steun ons, and the **About cluster** (hub/missie/visie/organisatie illustrated; **partners + pers** given cleaner text-only heroes for their more-serious tone). Draft aspirational titles in place for Frederik to refine. **Global consequence handled:** the fixed pill left flow site-wide, so non-hero pages (home, activity/chapter detail, news, find-a-bike, …) got a **`pt-28` pill-clearance on `<main>`** with the full-bleed blue bands (`.home-hero`/`.activity-hero`/`.chapter-head`/`.page-hero__spacer`) cancelling it via `margin-top -28`, plus more inner top-padding on the detail heroes — verified home/activity/chapter/plain pages all clear the pill. **Refinements (Frederik on the live page):** top-anchored hero copy (titles never clip the fold), **+breathing room** (hero 40rem desktop / 30rem mobile, generous top/bottom padding), and **crisp blue seam** — removed the panel's upward box-shadow (it cast a grey band over the blue) and overlapped the panel 2rem so the rounded corners sit on blue, not the page bg. **Verified:** full suite **142 green (516 assertions)**, Pint + `npm run build` clean, reduced-motion checked (entrance animations suppressed, nothing hidden), seam clean at scroll-0 and mid-scroll. **Committed** (15 commits `ee350b7…124ee95`, **not pushed** — Frederik/Nico push). **Notes:** `getting-started.blade.php` is Frederik's concurrent WIP (he re-adopted a scroll-stacking expectations band — left untouched; its `.gs-expect-scroll` CSS is in `app.css`). Dead old-hero CSS (`.index-hero*`, `*-hero__lead`, `about-hero*`, `gs-hero*`, …) left in place — harmless, deferred to avoid colliding with concurrent `app.css` edits. **Pipeline:** rows NOT bumped — Wire/UI stay 🟠, **🟢 gated on Frederik's own critique** of the new hero.

## [2026-06-03] build | Kalender (P-02) — `/critique` + `/arrange` `/polish` (width, hierarchy, featured event)

Frederik on the live Kalender: the featured event feels disjointed from the list, the page underuses the viewport width, and the hierarchy is unclear ("where do I look first"). Diagnosis: `.kal-body` was capped at **48rem and left-aligned** in a 1440 viewport (≈45% dead space); the date-grouped list was flat (no entry point); and the **Grande Kidical Mass had no featured treatment at all** — a plain card lost far down the list, which is exactly why it read as disjointed (the spec wants it inline-badged, "not a separate block"). Frederik picked **"widen + 2-column card grid"** over a filter rail. Built: **(1) width** — `.kal-body` uncapped → fills the container; cards flow in `.kal-day__cards` (`grid; repeat(auto-fill, minmax(min(100%,24rem),1fr))`) → 3-up on desktop, 1-up on mobile, a lone ride keeps a card-sized column. **(2) hierarchy** — date headers bumped to `clamp(2xl,2.2vw,3xl)`; the red "Dit weekend / Vandaag / Morgen" landmark leads the scan. **(3) featured** — `event-card` gains a `featured` prop (auto-detects "grande"/"grote kidical" by title until a real `is_featured` field exists — Nico) → inline `.event-card--featured` (light-yellow card, yellow border, **★ UITGELICHT** badge) in its chronological slot: featured *and* integrated. **Verified:** screenshots (desktop 3-up, mobile 1-up, featured card), full suite **138 + 1 new** (`EventCardFeaturedTest`: Grande badged inline, exactly once) green, Pint + `npm run build` clean. **Shared tree, no commit** — Nico is actively mid-restructure of `ride-calendar.blade` + the `.kal-*` CSS (the hero/filter moved into the component during this pass); my edits were surgical (uncap width, rename the cards div to `.kal-day__cards`, bump headers, featured card) but **flag for him to reconcile**. Data dep: a real `Activity.is_featured` flag (D-3) to replace the title heuristic.

## [2026-06-03] build | Chapter page (P-11) — built the Critique v3 family-first arc

Frederik: "implement as much as possible, fake the rest." Rebuilt `groups/show.blade.php` to the v3 skeleton. **Real:** **type-aware agenda** — reads the `activity_type` enum and renders one unified "Op de agenda" list with Fietstocht / Workshop / **Voor vrijwilligers** badges + per-type CTA; the next `kidicalmass` ride is the weighted hero, warm empty-ride state otherwise (kills the workshop-as-ride + meeting-as-ride bug, verified on Anderlecht's workshop + Brussel-Stad's meeting). **Filtered "Alle activiteiten (ook voorbije) →"** deep-link via `?gemeente={id}` (the `RideCalendar` `#[Url]` already supports it — not faked). **On-demand meehelpen reveal** (Alpine `x-show`, mirrors the event-detail pattern; auto-opens + welcome banner on `?intent=volunteer`) so the family page stays light. **News CUT** (was a big identical-national grid). **Opt-in promoted** beside the agenda (was buried under the news); closing slimmed to a quiet hand-back. **Faked (commented, removable):** lead + active volunteers **with roles** (`trekker` from the real pivot + faux `roze hesje`/`communicatie` — needs a `group_user.role` field), friends/downloads, client-side opt-in. CSS `.chapter-agenda*`/`.chapter-join*`/`.chapter-team__role` on the ride/show kit. **Tests:** `GroupsTest` updated to v3 + 2 new type-label tests (workshop ≠ ride; meeting → "voor vrijwilligers"); **full suite 138 green**, Pint + `npm run build` clean; screenshots verified (3 chapters + reveal + intent auto-open). Doc: `chapters.md` Critique v3 marked built; data deps for Nico (`group_user.role` + volunteers, per-lead routing #37, subscription model). Shared tree, no commit — `groups/show.blade.php` is in Nico's active set; flag for him.

## [2026-06-03] build | Kalender (P-02) — location-first filter (Flux searchable select via Livewire)

`/critique`: the gemeente filter was under-prominent and the page led with the Aankomend/Voorbije toggle, even though parents almost always want *upcoming* near *them*. Explored **3 directions as browser mockups** (`/critique` made a throwaway `public/_filter-mockups.html` on the real compiled CSS, screenshotted desktop+mobile — since removed); Frederik picked **A (prominent "Waar fiets je?" bar)** + asked for the **Flux searchable select**, ordered by postal code ("1030 – Schaarbeek"), searchable by zip **and** name. **Built:** the filterable list is now **`<livewire:ride-calendar>`** (`app/Livewire/RideCalendar.php` + `resources/views/livewire/ride-calendar.blade.php`) — `#[Url]` `gemeente`/`when` keep state shareable; the controller `ActivityController@index` is now just `return view()` (the component owns the query, rides-only + no-pagination + landmarks moved into it). Filter = `<flux:select variant="listbox" searchable clearable wire:model.live="gemeente">`, options `Group::visible()->orderBy('zip')` labelled `{zip} – {name}` (en-dash, not em); period demoted to a **"Bekijk voorbije ritten →"** `wire:click` link. **Flux JS prerequisite:** the public `site` layout loaded no Flux scripts (only Livewire/Alpine auto-inject), so added **`@fluxScripts`** before `</body>` — enables Flux interactive components site-wide (low risk; Flux CSS was already imported; nav stays direct-links, not reverted to a dropdown). Old `.kal-toggle*`/`.kal-select`/`.kal-filter__loc*` CSS removed; new `.kal-filterbar`/`.kal-locationfield`/`.kal-locationlabel`/`.kal-pastlink`/`.kal-days`. **Verified (Playwright, real reseeded NL data):** select opens, typing "schaar" → "1030 – Schaarbeek", **0 JS errors**; options ordered by zip; full **suite 136 green (486 assertions)**, Pint clean, build clean. NL-chrome test updated (toggle → "Waar fiets je?" + "Bekijk voorbije ritten"). Wire/UI 🟠 — Frederik final 🟢 gate. Shared tree, no commit.

## [2026-06-03] design | Chapter page (P-11) — `/critique` v3 on the live build + skeleton rethink (doc-only)

Frederik reviewed the **built** chapter page across three real cases (Schaarbeek = filled; Anderlecht = workshop, no team; Brussel Stad = meeting). Core diagnosis: the IA serves the *secondary* audience (would-be volunteer) first — a full-width always-open J2 form is the page's centre of gravity — while the *primary* family's job ("what's on, where can I go?") is muddled; and the build drifted from the v2 spec. Resolved (3 questions answered by Frederik): **(1) activity-type blindness** — `activity_type` enum (`kidicalmass`/`meeting`/`workshop`/`other`) was ignored, so a vrijwilligersmeeting + a fietscheck-workshop both showed as "Volgende rit → Naar de fietstocht" → **one unified type-labeled "events by de lokale groep" agenda**, rendering adapts per type, the soonest *ride* gets the weighted card, empty-ride state otherwise. **(2) team = lead + active volunteers with roles** (not the lonely lead) — needs volunteers + pivot `role` on `group_user` (Nico). **(3) national news CUT** (identical across all chapters via region-fallback; answers no "what's on near me"). Plus: J2 form → **on-demand reveal** (port the event-detail `x-data` pattern, auto-open on `?intent=volunteer`); **"mis geen rit" opt-in promoted** up next to the agenda (was buried under the news); **"Alle ritten →" must be FILTERED** to this group incl. past (build linked the bare index). Revised arc: identity → ride photo → typed agenda + opt-in → wie dit trekt (lead+crew) + on-demand meehelpen → closing. Recorded as **Critique v3** in [`chapters.md`](design/30-skeleton/chapters.md) + new Nico data deps (type-aware rendering, volunteers+roles, filtered events link, subscription model). **Doc-only**, no code (Nico's active build); grounded in DB (`activity_type` enum, Schaarbeek/Anderlecht/Brussel-Stad data) + the live pages.

## [2026-06-03] build | Event detail (P-03) hero date + STARTUUR; fix "loopfiets" copy (chapter page)

Two quick Frederik critiques on rendered pages. **Event detail (`activities/show.blade.php`):** the ride date hid down in the yellow meta panel while the hero title had no date. Added a **yellow date subtitle** under the H1 (`.activity-hero__date`, e.g. "Zondag 7 juni", `translatedFormat('l j F')` + ucfirst, reduced-motion-aware). Since the date now reads with the title, the meta panel's **"Wanneer" (date + time) → "Startuur" (time only, `14h00`)** with a clock icon — no more duplicate date. **Copy fix (`groups/show.blade.php` chapter identity line + `chapters.md` spec):** "iedereen welkom, van **loopfiets** tot bakfiets" was wrong — Getting Started already states *loopfietsen zijn niet geschikt voor de weg*, so advertising them contradicts the rule; changed to **"van kinderfiets tot bakfiets"** (a child's pedal bike is the honest small end). **Verified:** screenshot of the detail page (yellow "Zondag 7 juni" subtitle under the title; STARTUUR · 14h00 in the panel), render check of a chapter page ("van kinderfiets tot bakfiets" present, "loopfiets" gone), **full suite 136 green (486 assertions)**, build clean. Shared tree, no commit. (The chapter page itself is P-11's concurrent build — only the one copy line touched there.)

## [2026-06-03] build | Chapter page (P-11) — built the local-group "home" NL on the ride/show kit

Off the fresh re-plan ([`chapters.md` § Chapter Page](design/30-skeleton/chapters.md)) + an interview locking the data strategy, rebuilt `groups/show.blade.php` from the EN/off-kit flat stack into a **warm arc**: breadcrumb + region + **templated identity line** → full-bleed **shared ride-photo band** (`public/img/photography/ride-cinquantenaire-crowd.jpg`, no per-group cover yet) → **next-ride hero** (solid-blue kit card, "Naar de fietstocht →") with a compact "ook later" list → **team as faces** (sky initials avatars, names only, no role/photo field; the grey "Organiser" chip is gone) → **J2 form** (`ChapterVolunteerSignup`, reused) with empty-team warmth ("Help <stad> op gang") → **Uit de buurt** region-fallback news (hide-if-empty) → **faux** vrienden + downloads preview (press stays hide-if-empty, never faked, D-11) → **faked** "mis geen rit" opt-in (Alpine `x-data`, client-side "bedankt", does not persist). Cut: count badges, "Part of:", the "More" dump. **Designed empty state** ("Nog geen rit gepland" → opt-in) for truly-isolated groups; Brussels leaves always look alive because the controller aggregates parent-region rides (unchanged). New `chapter-*` namespace in `app.css` (appearance there; template = structure only); `[x-cloak]` added. **Decisions baked in (Frederik):** defaults + spec for Nico (no shared-schema migrations here), NL-only (FR toggle deferred), region/parent nodes get a minimal children list (full variant deferred), **cadence line dropped** (recurrence isn't reliable). **Verified:** render desktop+mobile on filled (Schaarbeek) + empty (Luik), 0 em-dashes rendered, `npm run build` clean, Pint clean, **suite 136 green** (4 new chapter-home tests + the mixed-content test updated to the new compact list). **Pipeline P-11:** UX 🟠→🟢, UI 🔴→🟠, Assets 🔴→🟠 (shared photo in); **Wire stays 🟠, 🟢 gated on Frederik's own critique.** `/build` parses clean (no warnings/drift). Plan: [`docs/superpowers/plans/2026-06-03-chapter-page-home.md`](superpowers/plans/2026-06-03-chapter-page-home.md). Shared tree, no commit. **Backend spec open for Nico:** per-group `intro`/cover/`role`/lead-email ([#37](https://github.com/ndeblauw/kidicalmass/issues/37)) + single-group `Email subscription`.

## [2026-06-03] build | Kalender (P-02) — structure pass: it's a calendar, so stop paging it

`/critique` of the overall structure (Frederik: "now we're just paging all the content — does that make sense, considering it's a calendar?"). Verdict: paging is the wrong model — a calendar of *sparse* events (≈5–10 upcoming at a time) is an **agenda list you scroll**, not pages, and the real list-shortener is the **gemeente filter**, not pagination. Confirmed the two parent jobs: (1) "is there a ride near us, next one?" → filter to area, soonest-first, top of list; (2) "which weekend works?" → scan a short run of upcoming dates in that area. **Built:** (a) **removed pagination** (`ActivityController@index` now `->get()`); upcoming shows the whole run soonest-first, **past = bounded recent archive** (`limit(24)`, newest-first, no paging); (b) **relative time landmarks** on imminent day headers — `Vandaag` / `Morgen` / `Dit weekend` (red, with the date following muted) via Carbon `isToday/isTomorrow/isCurrentWeek+isWeekend`, so the next ride and next free weekend pop; (c) confirmed the **town-led card headline** (prefix-strip) lands on real data — the agenda reads as a scannable column of towns (Laken · Ukkel · Elsene · …), one-offs ("Grande Grote Kidical Mass: najaarseditie", "Bright Light Parade Schaarbeek", "Spooky Edition Schaarbeek") keep full names. Design note: explicitly **not** a month-grid (it would be 90% empty at this event density); the date-grouped agenda is the correct calendar UI. **Verified:** screenshot (reseeded NL data) shows "Dit weekend" landmark, town column, continuous June→Oct timeline, no pager; **full suite 136 green (486 assertions)** incl. +2 (whole-run-no-pagination, relative-landmark), Pint clean, build clean, 0 em-dashes. Future (not now): "load more / by year" for the deep past archive beyond the 24 cap; a per-ride "zet in agenda" (iCal) + region calendar feed (needs Nico). Wire/UI 🟠 — Frederik's final 🟢 gate. Shared tree, no commit.

## [2026-06-03] build | Partners — consolidated "who backs us" (anchors + wall)

Resolved the critique's open question (4 institutional cards + 3 operational cards + logo wall = three heavy similar-weight blocks before the pitch). Frederik picked **"anchors + wall"**: merged into **one** section "Onze partners en bondgenoten" — the 4 **institutional named cards** (depth/context, the credibility anchors) directly followed by the **logo wall** ("En vele anderen…", breadth). The **operational/in-kind partners lost their dedicated cards**: they're a family/resource story (bike provision) that overlapped the wall and `/find-a-bike`, so they fold into the wall + a one-line pointer ("partners zoals Loopz en My Kids Bikes… Geen fiets? → find-a-bike"). Net: 3 blocks → 1, clear depth→breadth, the page reaches "Waarom partner worden" much faster. Added `.partners-page .partner-logo-wall { margin-top: 1.5rem }` to separate breadth from the named cards within the block. **Verified:** screenshots (one clean block, wall has air, in-kind line reads as a footnote), `npm run build` clean, **full suite 130 green** (updated the Partners-leaf test: new heading "Onze partners en bondgenoten" + "En vele anderen…"; "Loopz"/find-a-bike still asserted), Pint clean. Docs: `partners.md`. Shared tree, no commit.

## [2026-06-03] build | Seed data — replace faker lorem with real Kidical Mass content (`d9343c4`)

Rewrote `GroupSeeder` + `DatabaseSeeder` so the local DB reads like the real site instead of lorem-ipsum: **26 real chapters** (NL city names, postal codes, founding years) under the three regions; **24 activities** (real parades with true departure points, split upcoming/past so both Kalender tabs populate, plus autumn Grande + Bright Light + Spooky + meeting/workshop rows; the Grande carries the real parade photo); **9 back-dated news items** mirroring real press; **15 real partner records + NL descriptions**; 6 coordinator users; 6 plausible NL contact-form enquiries. Tone-of-voice clean, 0 em-dashes; factories keep their lorem defaults for tests; `migrate:fresh --seed` reproduces it. **No stage bumps** — seeding local data is not the `Back 🟢` bar (live CMS wired + verified). Touches **`D-11`** only as precision: the *records* half of the partner gap is now in hand; **logos + a `category` field + binding the page/partner-strip to the table** stay outstanding, so P-20 `Back` stays 🟠 (in-progress, unverified — not 🟢) and D-11 stays Open. Registry: P-20 row + D-11 roll-up updated. Verified via tinker counts + screenshots of `/nl/events`, `/nl/chapters`, `/nl/about/news`. Shared tree; seeders committed `d9343c4`, registry/log separate.

## [2026-06-03] build | Partners — `/critique` + `/polish` `/normalize` (finish defects)

Critique verdict: not AI-slop (real brand system, photography, logo wall, honest copy); the arrange pass landed (calm, connected, one CTA); remaining gap was *finish*. Fixed three: **(1)** the rotated **"OVER ONS" hero badge overlapped the two-line H1** ("Partners &") → read as broken → clearance via `.partners-page .about-hero__badge { margin-bottom: 1.75rem }` (scoped, doesn't touch other About leaves). **(2) Normalise:** card radii were **1.25rem** vs DESIGN.md's **1.5rem** content-card standard → bumped `.about-partner-card` + `.partner-enquiry__form` to 1.5rem (`.about-partner-card` is partners-only, safe). **(3)** the **logo wall was illegible on mobile** (one wide composite shrunk to specks, defeating "partnerlogo's te zien") → `min-width: 34rem` + `overflow-x: auto` so logos stay legible and scroll horizontally on narrow screens. Left intentionally: faint 10%-ink dividers (connected, not separated), the 7 partner text-cards (genuine content), the reused Press checkmark list. **Verified:** screenshots (badge clears title, cards rounder/consistent, mobile logos legible+scroll), `npm run build` clean, **full suite 129 green**, Pint clean. Docs: `partners.md`. Shared tree, no commit.

## [2026-06-03] build | Partners — `/arrange` pass (band assignment + section rhythm)

Frederik on the polished page: the light-blue **operationeel** band made it look like the most important section (it isn't); the white sections felt like floating islands ("ik heb niet het gevoel dat de dingen bij elkaar horen"); and the accent band probably belongs on the **CTA/form**. Fixes: **(1)** operationeel band → plain **white section** (no longer falsely elevated). **(2)** the single light-blue accent band **moved to the enquiry/CTA** at the page end — the actual primary action; the white form card pops on light-blue, blue submit button. **(3)** wrapped the page in `.partners-page` and gave the white body **one connected-column rhythm**: consistent `padding-block` + a **hairline divider** (`color-mix(ink, transparent 90%)`) between every consecutive body section, so the six sections read as parts of one document instead of islands. Net: blue hero → divider-separated white body → one light-blue CTA band (two intentional full-bleed bands total, vs the earlier scattered three). **Verified:** `npm run build` clean, screenshots (operational now white + cards reveal on scroll, dividers render, enquiry band is the clear focal CTA), **full suite 129 green**, Pint clean. Docs: `partners.md` arrange note. Shared tree, no commit.

## [2026-06-03] build | Kalender (P-02) — Frederik critique on the rendered page: fix filter clarity, card readability, duplicate date

Frederik reviewed the rendered Kalender (screenshot) and flagged three things, all correct: (1) the location filter on the right was **unclear** and made the page feel like it searched by *location* when it's organised by *date*; (2) the **event cards were hard to read**; (3) the **date was duplicated** — in the date-group header *and* on every card. **Playwright fixed first** (global module wasn't resolving from the project cwd; added `scripts/screenshot.cjs` that self-resolves the global install) so this was a screenshot-driven pass. **Fixes:** (a) the gemeente control is now a **clearly-labelled "📍 Gemeente" filter** (pin icon + visible label), visually secondary to the Aankomend/Voorbije period browse — removes the "am I searching by date or location?" ambiguity; (b) **`<x-event-card>` (PAT-1, shared) redesigned** from a square text block into a clean row; appearance moved into `app.css` `.event-card*` (was inline utilities), hover = lift + shadow (dropped the low-contrast orange title hover). **Hierarchy corrected after a 2nd Frederik critique same-day:** the first cut made *time* a big left rail, but a parent scans for a ride *near them* → **WHERE leads** — the ride title (municipality) is the bold anchor, meeting point + red pin second, and the **time is a small muted detail top-right** (date stays in the day header). **3rd critique (same-day):** every ride is "Kidical Mass &lt;town&gt;", so the repeated brand prefix buried the one scannable token (the town) → the card headline now **drops a leading "Kidical Mass "** (display transform in the component): "Kidical Mass Schaarbeek" → **"Schaarbeek"**; one-offs not starting with the prefix ("Grande Kidical Mass 2026") keep their full name. Chosen over sourcing the town from the linked chapter (no extra loading, can't mislabel the Grande, safe fallback); +1 test. (c) **duplicate date killed** via a new **`showDate` prop** — the Kalender passes `:show-date="false"` (the day header carries the date), while home/chapter keep `true` (and now render NL dates "WO 3 NOV" instead of the old English "WED"); (d) the date-grouped list is now a **single-column agenda** at the hero's readable measure (`.kal-body`, 48rem) instead of a 2-col grid that stranded lone rows at half-width. Also re-confirmed the **hero badge-overlap fix** is live (the badge had inherited `hero-chapter-in`, which ends `translateY(3.5rem)`; dedicated `badge-in` keyframe + flex-column inner — Frederik's screenshot predated that build). **Verified:** screenshots desktop + mobile of Kalender (clean agenda rows, labelled filter, no duplicate date, fixed hero) + home + a chapter page (shared card unbroken, NL dates); **full suite 129 green (458 assertions)**, Pint clean, `npm run build` clean, 0 em-dashes. Note: the shared-card redesign also improves home + chapter cards. **Reminder:** `npm run build` rebuilds assets only — Blade needs `php artisan view:clear` when a component changes under concurrent/caching conditions (bit me once this pass). Wire/UI still 🟠 (Frederik's final 🟢 gate). Shared tree, no commit.

## [2026-06-03] ux | Chapter page (P-11) — flagged as "not a home", re-planned from scratch + Impeccable critique

Frederik flagged `/nl/chapters/{group}` (`groups.show`): *"een pagina van een lokale groep — dat is echt nog niet goed uitgedacht. Heel messy. Dat hoort een thuis te zijn voor zo'n groep, en zo voelt het nu niet."* One of the most important things still to do — re-plan now, **don't build yet** (no todo). **Pipeline (P-11) honest regression:** the row claimed "Rebuilt + rebranded" but the live view is **EN, off the ride/show kit (raw inline brand colours), a flat stack of equal-weight sections** — it opens on metadata (zip + "X activities/X articles" count badges + "Part of:"), buries the next ride below a generic "Subgroups" grid, reduces the team to grey "Organiser" chips, ends on a catch-all "More" dump of empty placeholders. Regressed **UX 🟢→🟠, UI 🟠→🔴**; gap + Roll-up corrected; the built **J2 volunteer form survives** the re-think. **Fresh UX re-plan (on my own, Strategy→Skeleton)** in [`chapters.md` § Chapter Page](design/30-skeleton/chapters.md) around one idea — *the page is the local group's home; a resident should feel it is theirs.* Reframed three audiences (tentative family = primary; would-be helper; returning local), the central tension (**uniform frame, inhabited room** — character lives in contents, not chrome), led with **next-ride-as-hero**, cut count badges / "Part of:" / "More", made the **just-started empty state a first-class designed page**, and surfaced a **leaf-vs-region/parent fork** (open-Q #7). **Then ran the Impeccable `/critique` skill** on the v1 skeleton: confirmed the fixes and surfaced three "home/alive" gaps I'd missed — **(a) no photo of an actual ride** (the brand's strongest emotional asset → warm ride-photo band + shared fallback), **(b) rhythm invisible** (added "elke laatste zondag" cadence line), **(c) the home feeling over-loaded onto an editable identity line** leads may leave blank (re-seated on always-group-specific contents) — plus a microcopy fix (hero CTA "Ik kom →" → "Naar de fietstocht →", since it navigates). Folded into the v2 mockups + a `§ Critique v1 → v2`. Doc-only; 11 open questions for Nico/Frederik (per-group cover image, cadence field, editable identity line, leaf-vs-region fork, news/press aggregation, "mis geen rit" model). Shared tree, no commit.

## [2026-06-03] build | Partners — `/polish` pass (calmer bands, readability, normalised button, real imagery)

Frederik `/polish` on `/about/partners`: too many coloured bands for a page that may stay calm; the "Onze formules" sky band had unreadable dark-text + blue-link on medium blue; the enquiry submit used an off-system yellow Flux button; and the page lacked imagery ("misschien foto's waar partnerlogo's te zien zijn, banners in beeld"). Fixes: **(1) fewer bands** — the formules + enquiry full-bleed bands became plain **white sections**; only the operationeel band remains as a single accent. **(2) Readability** — formules now white, dark text + blue PDF link legible. **(3) Button** — `<flux:button>` (yellow) became the site's standard blue `.about-cta__btn--primary` on a native `<button>` (added `border:none;cursor:pointer` + `:disabled`). **(4) Imagery** — real **crowd-at-the-Cinquantenaire hero photo** (resized 11.6MB→233KB, `public/img/photography/`; new `.about-hero__circle--photo` modifier so the photo fills+covers the circle instead of the illustration's 80%/contain), and the real **partner logo wall** composite (`public/img/partners/partner-logos-2024.png`) as a `<figure>` after the operationeel section — the only cleared logo asset (D-11). **Verified:** `npm run build` clean, screenshots (hero photo fills circle, logo wall + formules legible on white, blue button), **full suite 129 green**, Pint clean, 0 em-dashes. Surface much calmer (1 accent band + hero + 2 real images vs 3 bands). Shared tree, no commit. Logo wall is a **2024** snapshot — refresh when the partner set changes; PDFs/prices still pending Leticia (D-11).

## [2026-06-03] build | About journey package — exits match intentions + contextual Steun

Frederik, after a `/critique` through the JTBD lens, asked two questions and then "build that package": (1) *if Steun is the #1 goal, why does no About page ask for it?* and (2) *what would the hub look like if it triaged by intention?* **In the shared tree, surgical, no commit. No stage-emoji change** (still Wire/UI 🟠 — Frederik critique pending); this is a content/UX refinement of already-built pages.

**The diagnosis behind it:** the About bodies served deciders but the *exits* served families — every leaf's primary CTA was "Vind een rit," and Steun (the #1 org goal) appeared only as the ambient header pill, never as a contextual ask at the moment About had just built the case for it.

**Built (three moves):**
1. **Hub intention triage** (answers Q2). New `.about-intent` strip above the 6-card browse menu: *"Waar ben je naar op zoek?"* → four cards routing to the **exits** deciders actually came for — **Meehelpen · Pers · Partners · Steun** — with *"Of lees meer over de beweging"* now labelling the content cards below. Hybrid, not a pure role-router (a phone-tree would exclude the largest group, the curious; the browse menu stays).
2. **Contextual Steun ask** (answers Q1). New reusable **`<x-about-support>`** (white tilted card, page-specific copy, the exact site-wide yellow Steun pill `.support-callout__cta` → `route('membership')`). Placed only where the case is hottest: **Missie** right after the impact stats (*"Al onze ritten zijn gratis"*) and **Organisatie** right after the no-paid-staff money model (*"Geen hoofdkantoor, geen betaald personeel"*). Deliberately **not** on Pers/Partners/Nieuws/hub-body — ask where the argument lands, stay quiet elsewhere (ToV: niet belerend).
3. **CTA retune** so exits match intentions: **Missie** closing CTA → primary **Help mee**, secondary "Vind een rit" (the family door demoted, not deleted); **Visie** → primary **Help mee**, secondary **Steun Kidical Mass**. Organisatie already decider-correct (Help mee / Voor het eerst).

Steun now has three contextual touchpoints (hub chip, Missie card, Organisatie card) + a secondary CTA on Visie — none preachy. **CSS** appended to `app.css` (`.about-intent*`, `.about-support*`); reuses the Steun pill for site-wide consistency. **Verified:** `npm run build` clean, Pint clean, screenshots desktop+mobile of hub/missie/visie/organisatie confirm the strip + Steun cards + retuned CTAs render on-brand; new **`AboutJourneyTest`** (+4: hub triage routes to exits, Missie/Organisatie Steun asks, Visie Steun CTA), **full feature suite 111 green (410 assertions)**, 0 em-dashes. Registry rows left as-is (no stage moved; Nico editing the file live). Not committed/pushed.

## [2026-06-03] build | Kalender (P-02) + Lokale groepen (P-10) — surface pass on the kit, then `/critique` + distill/arrange/harden

Applied the DESIGN.md look to the two index views (Frederik: "apply the design.md look and feel, then critique both UX and UI"), did a thorough `/critique`, and applied the fixes Frederik approved. **In the shared tree, no commit.** **First pass** put both pages on the ride/show kit (full `.activity-hero` poster + bands), appearance moved out of inline utilities into `app.css` (`.kal-*` / `.grp-*` deltas), honest placeholders kept (opt-in "binnenkort" band; map placeholder). **Critique caught** that a *poster hero* is wrong for *scanner/directory* pages (Kalender's own skeleton: "a scanner, not a storyteller") and several real issues. **Fixes applied:** (1) **`/distill`** both heroes → a **slim blue header band** (`.index-hero`: badge + −3° H1 + lead, no 460px illustration disc) so the list/directory isn't pushed below the fold; (2) **`/arrange`** Lokale groepen **list-first** — the region directory now leads, the map is demoted from an ~18rem panel to a slim "kaart komt binnenkort" note; (3) **`/harden` contrast (kit-wide)** — the sky badges rendered **white-on-`kidical-sky` ≈ 2.1:1 (fails AA)**; switched badge text to **`kidical-ink`** on `.kal-/.grp-/.gs-/.ho-/.about-hero__badge` (**touches the approved Help out / Getting Started / About / Mission pages — Frederik should re-glance**), and the `.grp-pill` hover no longer recolours text to low-contrast orange (border + lift only); (4) **CTA tone** — Lokale groepen's closing CTA is now a **calm contained light-yellow card**, not a loud full-bleed yellow band (recruiting is the quiet beat per strategy); (5) **sticky filter** — `.kal-filter` is now sticky under the header. **Left for Frederik's screenshot pass:** the `.activity-hero__chapter-label` (the big white-on-sky location label on the activity-detail hero) has the *same* contrast fail but is a prominent approved focal point — flagged, not changed unilaterally; sticky-filter top-offset wants visual tuning. **Screenshot verification (Playwright fixed):** global Playwright wasn't resolving via bare `require()` from the project cwd — fixed by resolving the global root, and left a reusable **`scripts/screenshot.cjs`** helper (self-resolves global playwright, HTTPS self-signed, `.cjs` for the ESM project) so `node scripts/screenshot.cjs` just works. Captured desktop + mobile of both pages. **Caught + fixed a real bug the screenshots revealed:** the slim-header badge had borrowed `hero-chapter-in`, whose keyframe ends at `translateY(3.5rem)` (built for the activity-detail chapter label), so with `both` fill the badge sat **56px down, overlapping the H1**; added a dedicated **`badge-in`** keyframe (ends `translateY(0) rotate(-3deg)`) and made `.index-hero__inner` a flex column. Re-shot: badge now stacks cleanly above the tilted H1 on desktop + mobile, ink-on-sky reads well, list-first directory + region pills + calm CTA all on-brand. **(NB: `.gs-/.about-/.ho-hero__badge` still use `hero-chapter-in` — same latent 3.5rem shift; left untouched as they're approved poster heroes with room, flagged for Frederik.)** **Verified:** **full suite 125 green (437 assertions)**, Pint clean, `npm run build` clean, both pages **200 · 0 em-dashes**, slim `index-hero` + sticky filter + slim map note + list-first + calm CTA confirmed in-render. P-02/P-10 top-gaps updated; Wire/UI still 🟠 — Claude render-check tops at 🟠, **Frederik's own critique is the gate to 🟢** (screenshots now exist for it).

## [2026-06-03] build | Partners — built the become-a-partner conversion flow (replaces the mailto black hole)

Frederik: "build it" (the plan from the prior entry). Replaced the mailto `<x-about-cta>` on `/about/partners` with the full conversion flow: **waarom partner worden** (benefit checklist from the Sponsorformules "waarom steunen") → **onze formules** (sky band, the two tracks — vzw's: Supporter/Partner/Community Partner · bedrijven: Friend/Sponsor/Main Partner — summarised by benefit, **no on-page prices** per the Leticia gate) → **wat we van partners vragen** (charter essence, doubles as a values filter) → **Interesse?** (light-blue band, two-column: warm intro + email/phone fallback | routed form). New Livewire **`PartnerEnquiry`** (PAT-6, mirrors `ChapterVolunteerSignup`): naam · e-mail · organisatie · type · formule-interesse · bericht → `ContactForm` + `ContactFormSubmitted` to the comms inbox, honeypot; routes-to-partnerships `TODO` (bike@ vs partners@, open). Both Wix PDFs **re-hosted** at `public/downloads/kidical-mass-{sponsorformules,partnercharter}.pdf` (serve 200) and linked as downloads. CSS: `.partner-formules` two-track grid + `.partner-enquiry` two-column on the About kit; selects reuse `.volunteer-signup__input`. **Verified:** screenshots (formules + form render clean), `npm run build` clean, **full suite 125 green** (new `PartnerEnquiryTest` ×3 + `PublicStructureTest` flow case), Pint clean, **0 em-dashes** in rendered copy (fixed two self-introduced: em-dashes in the formule list + select labels → colon/parens). **Provisional pending Leticia:** PDFs are the *KM Brussels* versions and tier names/prices await her national-scope OK before they're relied on (swap PDFs + confirm names). Surface = About kit (sky/light-blue bands, white form card, yellow submit) — Frederik visual critique pending. Docs: `partners.md` § conversion flow marked built. Registry P-20 left to Frederik's concurrent About-pipeline pass. Shared tree, no commit.

## [2026-06-03] design | Partners — "partner worden" black hole → conversion-flow plan (doc-only)

Frederik: the `/about/partners` "Zelf partner worden?" CTA "is too much of a black hole — you have to mail for info," and supplied the two live Wix PDFs to review. **Correction:** my earlier claim that Sponsorformules/Partnercharter "don't exist" was **wrong** — both are real, structured, bilingual docs (the homepage just linked email CTAs, not the PDFs). Fetched + captured both verbatim in [`partners.md`](design/30-skeleton/partners.md): **Doc A = Sponsor-/Partnerformules** (two tracks, €/yr excl. VAT — vzw's: Supporter €100 / Partner €250 / Community €500; bedrijven: Friend €500 / Sponsor €1.000 / Main Partner €2.500, each = logo + N social-vermeldingen + flyer/banner/stand visibility); **Doc B = Sponsor-/Partnercharter** (values + terms; non-political/non-commercial, contributions without strings, right to refuse, GDPR). **Analysis:** the audience is evaluative/B2B (not the warm `/steun-ons` family ask), so transparency is the conversion lever — showing price+benefit lets a prospect self-qualify before contacting; a bare mailto adds friction at exactly the pre-qualification moment, killing the €100-tier civic supporters. It also resolves the IA split `steun-ons.md` half-decided: **`/steun-ons`** = families · recurring · Growfunding · €3/mo; **`/about/partners`** = orgs · annual · **invoiced agreement** (the "excl. VAT / per agreement" wording ⇒ *not* Growfunding) · €100–2.500/yr. The charter doubles as a **filter** (selectivity signals "chosen partner"). **Recommended flow:** social proof (existing cards) → waarom partner worden → formules summary → charter essence + download → routed form. **Locked decisions (3 Qs):** (1) **scope = placeholder pending Leticia** — these are a *KM Brussels* artifact, build the structure but treat prices/tier-names as provisional until she confirms national applicability; (2) **depth = summary on-page + downloadable PDFs** (re-hosted off Wix), keeps the price table + national-pricing risk off the template; (3) **contact = routed form (PAT-6) primary, email/phone fallback**. **Open deps:** Leticia national-scope OK (blocks live prices), PDF re-hosting, form-routing inbox (`bike@` vs `partners@`), price-maintenance owner. **Doc-only:** updated `partners.md` (corrected the "don't exist" section + new § conversion flow with verbatim content), `about.md` (P-20 "become a partner" rewritten), `01-concerns.md` (D-11 Partners bullet extended). Registry P-20 left to Frederik's concurrent About-pipeline pass (active in that file). No code.

## [2026-06-03] fix | "Over ons" nav — inert Flux dropdown → direct /about hub link

Frederik: "the about-us dropdown doesn't seem to work. Maybe rather a hub page?" **Diagnosed:** the public layout ([`layouts/site.blade.php`](../resources/views/layouts/site.blade.php)) loads **no interactive JS** — `resources/js/app.js` is empty and there is **no `@fluxScripts`/Alpine/Livewire** (only the auth/admin layouts include `@fluxScripts`). The desktop `<flux:dropdown>` "Over ons" menu rendered its `data-flux-dropdown` markup but had nothing to open it → the six sub-pages were unreachable from the desktop nav. **Fixed (header.blade.php):** replaced the dropdown with a direct `<flux:navbar.item href="{{ route('about') }}">` — the **hub page (P-14) IS the sub-page menu** (six cards), this matches what mobile already did, works with **zero JS**, and is more accessible/crawlable than JS-gated links. Active-state logic unchanged (`routeIs('about','about.*')||routeIs('articles.*')`); footer still lists all six leaves for 1-click access. **Verified:** rendered nav has `data-flux-dropdown` count 0, "Over ons" is an `<a href=".../about">` with `data-current` on About pages, nav screenshot clean (no orphaned chevron); NavigationTest +1 regression guard ("links Over ons straight to the hub, no dropdown markup"), **Navigation + PublicStructure 36 green**. Not committed/pushed.

**Wider finding (flagged, not yet acted on):** the same no-JS-on-public-layout root cause means the **mobile hamburger toggle** (`x-data`/`x-show`) and the **scroll-shrink header** (`:class="scrolled?…"`) are also inert — Alpine never loads. On mobile the `x-show` nav has no `display:none` applied, so the links sit **always-expanded** (usable but unintended); the logo never shrinks on scroll. Decision for Frederik/Nico: either add `@fluxAppearance`+`@fluxScripts` (+Alpine) to the public layout to power these, or convert them to CSS-only / a tiny vanilla toggle. Out of scope for this fix (touches the global shell + JS strategy).

## [2026-06-03] ux+build | Over ons (About section, P-14..P-20) — journey + JTBD, then the full NL build

Frederik: "the About-us section is not developed well yet. What core user journey of jobs-to-be-done rely on it. Review the journey, then do thorough UX planning, by your own judgement — just log questions and concerns. End state: a logical user journey with working pages + all remaining About pages, working nav, links in all logical spots, good NL copy, DESIGN.md layout, taken through the impeccable critique skill and improved, screenshot-verified with Playwright." **In the shared tree, surgical, no commit.**

**UX planning (the new layer).** `about.md`/`about-content.md` already held per-page 5-plane specs + EN/FR/NL copy; the missing piece was the **consolidated journey + jobs-to-be-done**. Wrote [`30-skeleton/about-journey.md`](design/30-skeleton/about-journey.md): About is **not** a family-acquisition surface — it serves **deciders & deepeners** (prospective chapter leads, partners, funders, journalists, proud participants) whose one core job is *"should I invest in / commit to this movement?"*. Six JTBD rows, each mapped to the leaves it leans on and the **exit it needs** (the deciders convert *off* About — into help-out, contact, steun, events), plus a **cross-link map** that turns the corridor into a journey, the Skeleton build-deltas, and a consolidated **open-questions** list.

**Build (all NL, on the ride/show kit).** Replaced the six `<x-stub>` leaves with real pages built from `.activity-hero*` / `.activity-promises*` / `.ho-deal*` + a small `about-*` CSS namespace in `app.css`; two shared components **`<x-about-cta>`** (forward CTA) + **`<x-about-reveal>`** (opacity-only scroll stagger, reduced-motion-safe). **Over ons** (hub: orient + 6 nav cards + mini stats + CTA), **Missie** (intro · 3 axes Start/Support/Spread · stats · inclusivity → Getting Started · Julienne pull-quote), **Visie** (position statement · 4 numbered demands · manifest link · 2 parent voices), **Organisatie** (intro · semantic 3-tier organigram · national-vs-local two columns · named coördinatieduo Leticia & Cecilia · safety → GS), **Pers** (contact-forward + honest), **Partners** (curated static), **Nieuws** NL surface pass. Nav was already wired (dropdown + footer + hub cards + inter-page links); home entry deferred (still the EN placeholder). 0 em-dashes throughout (ToV).

**Honesty (→ new OPEN concern `D-11`).** The two credibility leaves have **no real data**: `partners` is faker rows with no logos and **no category column**; there is **no Press model**. So **Partners** ships from **curated static NL copy** (real names, no lorem) and **Press** ships **contact-forward** (`bike@kidicalmass.be`) with an honest empty state + a truthful **"eerder verschenen in"** outlet strip (RTBF/BX1/BRUZZ/La DH/HLN/Het Nieuwsblad — no fabricated links). Logged `D-11` in [`01-concerns.md`](design/01-concerns.md) (Nico enriched it: the site-wide `partner-strip` also finds no national records → only the hardcoded BM logo shows).

**Critique + improve.** Ran the impeccable `/critique` skill against desktop+mobile screenshots: **passes the AI-slop test** (inherits the distinctive house system — Fredoka/Poppins, −3° tilts on hero/badge/chips only, red rounded chips, full-bleed colour bands, daisy). Three concrete fixes applied: (1) Pers got the truthful outlet strip (was the weakest page — a journalist wants proof *now*); (2) Partners' in-kind row now fills via `auto-fit` (was a ragged gap); (3) hub nav cards — stronger description contrast + hover-slide arrow.

**Verified:** `npm run build` clean, Pint clean, **full feature suite 102 green (350 assertions)** — rewrote the obsolete `it marks stub pages as unfinished` test into **8 About tests** (real NL sections, forward links, no-em-dash, no-lorem, honest empty states); Playwright screenshots desktop (1440) + mobile (390) for all 7 routes, **0 JS errors**; live render of all 7 routes **200**; `/build` dashboard report **warnings [] · drift []**. **Stage bumps (honest):** P-14/15/16/17/19/20 Wire 🔴→🟠, UI 🔴→🟠; P-18 note refreshed; **Wire/UI 🟢 stays gated on Frederik's own critique** (Claude render/tone check tops at 🟠). Partners/Press **Back blocked on `D-11`**; Mission `Back` ❓→⚪ (live PAT-4 stats deferred, static for now). Registry rows + roll-up + `D-10`/`D-11` lines updated. Not committed/pushed.

## [2026-06-03] build | Kalender (P-02) + Lokale groepen (P-10) — NL build pass off the re-plan

Built the two index views from the just-refreshed skeletons (Frederik: "do the build pass"). **In the shared tree, surgical, no commit.** **Kalender (`activities/index.blade.php` + `ActivityController@index`):** was an EN wireframe stub (placeholder filter, all activity types, no date filter). Now **NL + wired** — controller filters **rides-only** (`activity_type = kidicalmass`, the D-2/J1 decision; meetups stay off the national list), an **Aankomend/Voorbije toggle** (`?when=`, links) + a **Gemeente filter** (`?gemeente=` group id, GET form with `onchange` submit + `<noscript>` fallback) both preserving each other's state and surviving pagination (`withQueryString`), NL date-group headers (upcoming = day, past = month, via `Carbon->locale('nl')->isoFormat()` + `Str::ucfirst`), and NL empty states (no-filter / per-gemeente / past). The **"mis geen fietstocht" email opt-in** is an honest **`<x-wire.placeholder>`** band after the list (no `Email subscription` model yet — not faked). **Lokale groepen (`groups/index.blade.php`):** **reverted the card-grid drift** (cards + "X activities · X articles" badges + "Part of:") → NL **region-grouped name links** — region = the invisible parent group, ordered **Brussel → Wallonië → Vlaanderen** (label-mapped from "Brussels Capital Region"/"Wallonia"/"Flanders"), parentless groups bucket to "Overige groepen" so factory/edge data still renders; links are padded pills (the critique's mobile tap-target fix); the map stays a placeholder (Leaflet + coords pending); real **"Begin een lokale groep"** CTA → Help out (the quiet closing beat). **Tests:** `PublicPagesTest` beforeEach activity set to `KIDICALMASS` (so the rides-only filter doesn't make it flaky) + **5 new** (NL chrome/no-em-dash, rides-only-not-meetups, upcoming/past toggle, gemeente filter, region headers); `GroupsTest` "Part of:" assertion repointed to the new region-header behavior. **Verified:** **full suite 120 green (398 assertions)**, Pint clean, `npm run build` clean, live render of `/nl/events`, `/nl/events?when=voorbije`, `/nl/chapters` all **200 · 0 em-dashes** with every NL string + all 3 region headers present on real seed data. **No stage-emoji bump** — Wire 🟢 is gated on Frederik's own critique (Claude render-check tops at 🟠); P-02/P-10 top-gaps updated (now "built", remaining gaps = opt-in backend + map + surface pass + critique). `[back]` for both: opt-in subscription model (P-02) and the chapter map (P-10) are Nico's. No screenshot (playwright unavailable).

## [2026-06-03] design+build | Tilt rule rooted in DESIGN.md + Steun copy de-threated

Frederik (screenshots of the steun funds + CTA titles): "I don't want this 3-degrees tilted text for smaller titles. Fix and fix at the root in DESIGN.md." **Root fix (DESIGN.md):** the −3° tilt is now explicitly **reserved for the hero h1, the badge, and the icon chips** (plus the alternating card tilt); **section/band titles stay upright**. Updated the signature-moves rule, the promises-band row ("big **upright** H2"), and the new-page checklist. **Code:** removed the tilt from the four offending title selectors in `app.css` — `.steun-funds__title` (−2°), `.steun-cta h2` (−3°), `.support-callout__title` (−1°), and the pre-existing `.gs-cta h2` (−3°, Getting Started) so the rule holds everywhere. Confirmed tilt-free in the built CSS. Hero h1, badges, chips and cards keep their tilt. **Copy (separate Frederik note):** the steun hero lead *"Iedereen fietst gratis mee. Jij zorgt dat dat zo blijft."* read as a threat/guilt. Reworded to **"Meefietsen is gratis voor elk gezin. Jouw steun houdt de wielen draaiend."** (credits the giver, no obligation). Same spirit on the event callout: *"Help dit blijven bestaan"* → **"Steun de volgende rit."** Free-reassurance panel + home callout (already positive) kept. Test assertion updated to the new lead. **Verified:** `npm run build` clean, Pint clean, my tests green, live render shows new copy + no threat line. **One unrelated red test:** `it marks stub pages as unfinished` now fails because **Nico concurrently built `/about/mission` (+ vision, partners)** so it no longer shows "Stub" — his expectation to update, not caused by this change. Not committed/pushed.

## [2026-06-03] build | Steun page (P-04) — /critique + /normalize to the ride/show kit

Ran `/critique` on the just-built support page, then `/normalize` (Frederik). **Critique verdict:** not generic-AI, but **off-house-style** — the page had reinvented a parallel `.steun-*` system (bespoke hero, flat bordered funds cards, custom bands) instead of reusing the kit, violating DESIGN.md's "reuse `.activity-hero*` / `.activity-promises*` before writing new CSS." **Normalized:** rebuilt `steun-ons.blade.php` on the kit — hero reuses `.activity-hero*` (daisy, circular `kid-on-bike` illustration, red "Steun ons" badge, −3° H1); "wat jouw steun mogelijk maakt" reuses `.activity-promises*` (sky band, **white tilted cards + canonical red Flux-icon chips**), funds restructured to title+body cards in `lang/nl/support.php`; ask on contained white (white tilted card + light-blue free-reassurance panel); page closes on a full-bleed **yellow CTA band** (movement scale + a second Growfunding shot). Colour story **blue → sky → white → yellow**. **CTA consistency (the other normalize target):** the support pill is now the **yellow token** everywhere it's an entry point — nav, footer, **callout** (was ink; callout band changed to light-blue so the yellow pops), and the page ask; the closing band keeps the blue band-button (kit convention — yellow-on-yellow won't read). Removed the bespoke `.steun-hero`/`.steun-funds__chip`/`.steun-scale`/`.steun-tiers` one-offs; `.steun-*` is now thin deltas on the kit (mirrors how `gs-*` extends it). Reveal animation is **opacity-only** so it doesn't flatten the CSS card tilt. **Verified:** `npm run build` clean, **full suite 108 green**, Pint clean, live render (kit classes `activity-hero`/`activity-promises__item` present, key strings, **0 em-dashes**, yellow callout pill on home), dashboard 22/0/0. **Caching note:** a transient em-dash test failure was a **stale compiled view** from Nico's concurrent partners-strip edit (he moved the HTML comment to a `{{-- --}}` Blade comment); cleared with `optimize:clear`, not a code issue. P-04 + roll-up + steun-ons.md updated. Still Wire/UI 🟠 (no screenshot) — Frederik critique pending; not committed/pushed.

## [2026-06-03] build | Partners & sponsors — reduced the global band to a slim recognition strip ([PAT-5](design/40-patterns.md))

Frederik: "do the UX planning for the partner part of the footer ... I have a hunch the huge Partners & sponsors section can be reduced, and 'ook ondersteund door' moved to the About section." **UX planning (3 decisions answered):** keep a **slim funder strip site-wide**; funder-visibility obligation is **satisfied by the homepage** (so site-wide is a generous choice, not a must); the **"Sponsorformules · Partnercharter" links were investigated** → they **do not exist** (the raw Wix scrape shows the original used two `mailto:` CTAs, not documents — `raw/website/index.md`), so they must not ship as links and become a coördinatieduo content dependency. **Diagnosis:** the old `<x-partners>` was a full-bleed band rendered globally (`layouts/site.blade.php`) on **every** page, conflating recognition + acquisition (dead `#` links) + a 420px illustration; it duplicated `/about/partners` and competed with the [PAT-10](design/40-patterns.md) "Steun ons" ask. PAT-5 was always scoped to specific pages, not a global band — the build had drifted. **Built (in-place, shared tree, no commit):** rewrote `components/partners.blade.php` to a slim `<aside class="partner-strip">` (label "Mede mogelijk gemaakt door" + BM "Met de steun van" logo + muted partner-logo row + one link → `about.partners`); slimmed `lang/nl/partners.php`; replaced the heavy `.partners-*` band CSS with a compact `.partner-strip` block in `app.css`; re-scoped the `about/partners` stub to absorb the moved content (funder block, "Ook ondersteund door" supporters list, categories, contact-CTA "partner worden"). **Verified:** `PublicStructureTest` +1 (strip renders site-wide, links to `about.partners`, and acquisition/supporters copy no longer leaks onto every page); fixed a self-introduced regression (an em-dash in a new HTML comment broke the global `assertDontSee('—')` on Help-out/Steun — switched to a Blade comment); **PublicStructure 24 + Navigation/PublicPages 13 green**, Pint clean. **Docs:** new brief [`30-skeleton/partners.md`](design/30-skeleton/partners.md); PAT-5 reworked to two scopes; About/Partners ([`about.md`](design/30-skeleton/about.md)) absorbs the moved content + the Sponsorformules/Partnercharter finding + a flagged **data gap** (no category field on `partners`). No screenshot yet (visual check is Frederik's) → Wire/UI 🟠 ceiling. `/critique` pass follows.

## [2026-06-03] design | Kalender (P-02) + Afdelingen (P-10) — UX re-plan + `/critique` pass (doc-only)

Re-ran UX planning on the two index pages at Frederik's request ("use your own judgement, no questions yet, critique the skeleton"). The April skeleton docs were solid but predated four shifts and the live build had drifted. **Kalender (`events-overview.md`):** reframed to the live NL nav label **"Kalender"** (was "Ritten"); **homed the per-region "mis geen fietstocht" email opt-in** here (resolves old open-Q #4) as a calm band *after* the list, contextual to the active location filter, promoted to primary in the season-break empty state; made **rides-only** explicit (meetups never appear, D-2/J1); refreshed the desktop ASCII to NL (Aankomend/Voorbije · Gemeente · Uitgelicht · NL date headers). **Afdelingen (`chapters.md`):** reframed to the live NL nav label **"Afdelingen"** (flagged the glossary's warmer "lokale groepen" as open-Q #7); corrected **Liège + Mons from external ↗ pins to hosted first-class pages** (per the 2026-06-02 structural decision); **locked "names + links only, no count badges"** against the live build's drift into a card grid with "X activities · X articles" badges (open-Q #6, blocks a faithful build); reframed the map's primary job to grants + finding existing groups (recruiting leads = secondary, the quiet closing-CTA beat). **`/critique` pass** (skeleton-stage, so AI-slop/colour/type checks N/A): caught two real voice defects in my own placeholder copy and fixed both in-doc — off-glossary **"rit" → "fietstocht"** (concept noun), and a **banned em-dash** in the Afdelingen CTA copy split to two sentences. Also flagged: pre-existing em-dash in the April `*-content.md` companions (needs its own purge) and the 45-municipality list's mobile tap-target risk. **Term question resolved same-day (Frederik): "Afdelingen" → "Lokale groepen"** (warmer, glossary-approved, the duo's own grassroots word) — applied site-wide: `lang/nl/nav.php` `nav.chapters` → "Lokale groepen", `NavigationTest` assertion updated, `chapters.md` (title/H1/mockup/open-Q#7) + P-10 registry reconciled. **Verified:** `NavigationTest` 5 green (17 assertions); `/build` parses clean (0 warnings, 0 drift). Otherwise no code / no stage-emoji change (UX was already 🟢 on both); P-02/P-10 top-gaps carry the new build-affecting findings (opt-in to build; card-grid drift to revert; NL pass). Shared tree, no commit.

## [2026-06-03] build | Steun flow (P-04) — built the support page, nav CTA, footer relabel + contextual callouts

Built the fully-specced support UX (Frederik: "Build this"). **In-place in the shared tree, surgical, no commit/push** (Nico commits concurrently). **Terminology:** new `lang/nl/support.php` holds all "steun" copy; footer CTA `footer.membership_cta` "Word lid" → **"Steun Kidical Mass"**; `common.membership` "Lid worden" → "Steun ons". **Route:** `Route::view('steun-ons', 'steun-ons')->name('membership')` (name kept so all `route('membership')` links work); old `/membership` **301s** to `/steun-ons` (`membership.legacy`); `config/build.php` pages-map + briefs-map updated to `/steun-ons` + `steun-ons.md`. **Nav (`header.blade.php`):** the `@guest` login button is replaced by a **`♥ Steun ons`** accent anchor (`.nav-support`, yellow pill, red heart) shown to all; `@auth` keeps the user menu beside it; **login removed from the header** (already in the footer bottom bar); mobile menu gets the CTA **pinned first** (`.nav-support--mobile`), login item dropped. **Page (`steun-ons.blade.php`):** blue full-bleed hero (-3° title + free-stays-free lead) → "wat je steun mogelijk maakt" (3 cards, red Flux-icon chips, dynamic `<flux:icon :name>`) → ask card "Steun vanaf €3 per maand" + t-shirt token + ink Growfunding CTA, paired with a light-blue **free-reassurance** panel → tiers link → full-bleed yellow **movement-scale** band ("honderden gezinnen", **no backer count**). One-off path **omitted** pending D-9 (no IBAN published). Growfunding link is **locale-aware** (`/{locale}/projects/kidicalmassbelgique`; nl + fr both verified 200). **Contextual callout:** new `<x-support-callout variant>` (PAT-10), two NL copy variants both carrying the **riding-stays-free** clause, placed on **Home** (after news) + **event-detail** (after the organizers section, the warm "just rode" beat). All appearance in `app.css` (`.nav-support` / `.support-callout` / `.steun-*`); Blade stays structural; raw headings; 0 em-dashes. **Verified:** `npm run build` clean, **full suite 107 green** (NavigationTest +2: header CTA + `/membership`→`/steun-ons` 301; PublicStructureTest +2: page render + home callout; PublicPagesTest +1: event callout), Pint clean, live curl (steun page 200 + all key strings + 0 em-dashes; `/membership` 301; home callout + nav "Steun ons" present, "Word lid" gone). **No screenshot** (playwright unavailable) → Wire/UI 🟠 ceiling, Frederik critique pending. **Registry:** P-04 Wire 🔴→🟠, UI 🔴→🟠, Assets 🔴→⚪, Back ❓→⚪; row + roll-up (surface-done / back / routed / global-shell lines) updated. **Surface note:** lighter band pass, not a full ride/show-kit re-skin like Getting Started/Help out — Frederik may want that depth. FR `lang/fr/support.php` pending the FR layer.

## [2026-06-03] design | Flagged D-10 — page metadata / SEO / social share previews (cross-cutting, Open)

Noticing Help-out's page title is the bare, unsuffixed "Meehelpen", Frederik asked to flag the wider gap. Inspected the public `<head>` (`layouts/site.blade.php`): it's a stub — bare `<title>` with one fallback, a **hardcoded English** `<meta name="description">` reused site-wide, and **no Open Graph / Twitter Card / canonical / `hreflang` tags** at all, so shared links (a ~75 %-mobile, WhatsApp-heavy audience) render with no preview. Affects **every page**, so it's tracked as a cross-cutting concern, not a per-P gap. Opened **`D-10`** in [`design/01-concerns.md`](design/01-concerns.md) (Build isn't open yet, so build-time decisions still ride along as design concerns per the build plan): split into a **build mechanism** (head-metadata partial driven by per-page props + title pattern + locale `hreflang`, couples to D-7; graduates to `build/01-concerns.md` when Build opens 2026-06-16/17) and a **design/content** side (title pattern, per-page descriptions in voice, OG-image-per-page-type — events want their own). At-a-glance gate flipped (Open 0→1); conclusion gate updated. Mirrored as a cross-cutting **OPEN** roll-up bullet in the [`/build` page registry](design/30-skeleton/00-page-registry.md) with a "do not call launch-ready until resolved" note. Doc-only; no code or row-stage change.

## [2026-06-03] build | Help out (P-13) — Wire/UI 🟢 (Frederik-approved)

Frederik approved the re-skinned Help-out page ("Wire/UI can be green. Well done."). Pipeline bump via `/pipeline`: P-13 **Wire 🟠→🟢, UI 🟠→🟢** (honesty gate met — Frederik did his own critique + refine pass across the re-skin). `OK` stays 🔴 (client sign-off pending). **First page to reach Wire/UI 🟢.** Registry row + roll-up reconciled (new "Wire/UI 🟢" bullet; Help out pruned from the "surface-pass-done 🟠" and "wireframe-fidelity" bullets; "no page is 🟢 yet" corrected). Dashboard parses clean (no warnings/drift). Committed.

## [2026-06-02] build | Help out (P-13) — re-skin to the ride/show kit + real photos + copy joy

`/critique` of the first Help-out surface pass (Frederik, comparing the live Wix page): "the original gives a lot more happy vibes via text and photos." Diagnosis: my pass had (a) **no photography** (the original's hero photo + collage are most of the joy), (b) was **off the design system** — it used the warm-gradient + emoji-chip + contained-panel approach that was already rejected for Getting Started and that DESIGN.md marks as wireframe-placeholder, and (c) **cooler copy**. **Re-skinned to the ride/show kit** per DESIGN.md's new-page checklist: hero now reuses `.activity-hero*` (solid blue, daisy, **real volunteer photo** in the circle, "Doe mee" sky badge, -3° white headline); roles reuse `.activity-promises*` (yellow band, white tilted cards, **red Flux-icon chips** — shield-check/calendar-days/megaphone/camera/musical-note, emoji gone); added a **real-photo joy band** ("Dit is de bende", 4 photos, tilted) as the original's happy signal; the group picker is the climax on a light-blue band; quiet "start a group" coda. **Copy warmed up** (joyful, grounded, no emoji, 0 em-dashes): hero lead "Word deel van de ploeg die elke rit tot een feest maakt", warmer role + intro copy. **Photos** pulled from `docs/raw/assets/photography/` into `public/img/volunteers/` (6 files). All `.ho-*` CSS rewritten as thin deltas on the reused kit; old gradient/emoji block removed. **Verified:** `npm run build` clean, live render 200 (all bands + photos + red Flux chips render; debug bar confirms 0 exceptions; **0 em-dashes**), full suite **102 green**. **No screenshot** (playwright unavailable) — visual check is Frederik's; UI 🟠 ceiling. **Registry:** P-13 **Assets 🟠→🟢** (photos sourced; role illustrations moot — canonical Flux chips), UI stays 🟠; row + roll-up rewritten to the ride-aligned description. Wire/UI 🟢 still gated on Frederik's own critique. `[asset]` photo selection/cropping is a first pass — Frederik may want to curate.

## [2026-06-02] design | Steun flow — resolved the two open critique items (doc-only)

Closed the two items the `/critique` left for Frederik. **#3 nav label:** the support CTA is **"♥ Steun ons"** (heart icon) — the heart + verb-object disambiguate the *give* action from **Meehelpen** (volunteer); warm, not "Doneer". **#4 mobile (75% of traffic):** the button is **pinned as the first, accent-styled item at the top of the hamburger menu** (not buried, not a floating sticky that would compete with "vind een rit"). Applied to `20-structure.md` (nav model), `40-patterns.md` (PAT-10), `30-skeleton/steun-ons.md` (strategy note). Both critique items now resolved; the support UX plan is fully specced through Skeleton. Still doc-only — build backlog unchanged (terminology sweep + "♥ Steun ons" nav CTA + footer login move + `/steun-ons` page + contextual partial).

## [2026-06-02] design | DESIGN.md — documented the page-construction layout system

After the Getting Started surface pass needed a full reverse-engineer of the ride/show page to get the layout right, Frederik: "would it make sense to update DESIGN.md so future UI work is right quicker?" Yes. Added a **"Layout system (how a page is built)"** section + a **new-page checklist** to `DESIGN.md` (the file the impeccable skill loads first). It captures what was previously only implicit in `activities/show.blade.php`: the **full-bleed colour-band mechanic** (`100vw` breakout + nested `.container` re-align; first band `-8` top, last band `-8` bottom), **colour bands as a playful toolkit** (Frederik: deliberately **not** a fixed colour/section order — pages should vary; the activity sequence is one example, not a template; only soft guidance = adjacent bands distinct + page coherent), a **"reuse the section components, don't reinvent"** table (`.activity-hero*` / `.activity-promises*` / `.activity-info-map*` / `<x-event-card>`), the **canonical icon chip** (red rounded-square + white Flux icon; emoji = wireframe placeholder only), card spec (white, 1.5rem, alternating tilt), motion (IntersectionObserver stagger + reduced-motion opt-out), and the appearance-in-`app.css` rule. Also corrected the stale "card radius 0.75rem" line (content cards are 1.5rem; 0.75rem is the meta panel). Doc-only; no code or registry stage change.

**ROI decision (Frederik):** confirm + keep the volunteer charter public, but **don't link it on the public Help-out page** — only in volunteer onboarding / the logged-in back-office (D-1), and **host as PDF later**. Applied: removed the `vrijwilligersafspraken` Google-Doc link from the "wat we vragen" list (the bullet now just names the agreements: "Onderschrijf onze afspraken rond vriendelijkheid en veiligheid"); resolved help-out.md open-Q2; updated help-out-content.md. **Surface/UI pass (frontend-design skill):** applied the Kidical Mass design language (DESIGN.md) to the Help-out wireframe, mirroring the Getting Started precedent — **all appearance in `app.css` (`.ho-*`), Blade stays structural**, copy unchanged. Moves: full-bleed **warm gradient hero** (`person-with-boombox` illustration, -3° tilted headline, barrier-dissolver as the lead), **invitation role cards** with light-yellow -3° tilted emoji chips + hover lift, a full-bleed **light-yellow "wat meedoen inhoudt" panel** (two columns, green-check "krijgt" vs red-chevron "vragen" markers), the **group picker as the climax** (white pill grid, hover lifts to blue), and a deliberately **quieter "start a group" coda** (hairline-topped, muted, smaller heading — answers the critique's "secondary exit competes with the CTA"). Reuses the shared hero keyframes; hero elements added to the `prefers-reduced-motion` opt-out. **Verified:** `npm run build` clean (CSS bundle has the `.ho-*` rules), live render OK (all sections + illustration present, **0 em-dashes**, 0 Google-doc links), help-out test green. **No screenshot** (playwright unavailable here) — visual/aesthetic check is Frederik's; my render check tops out at **UI 🟠**. **Registry:** P-13 **UI 🔴→🟠** (Wire stays 🟠); row + roll-up (Surface-pass-done list) updated. Wire/UI 🟢 still gated on Frederik's own critique.

## [2026-06-02] design | Steun flow — /critique + fixes (doc-only)

Ran `/critique` on the just-documented support plan (design-plane, no UI yet), then applied Frederik's chosen fixes to `30-skeleton/steun-ons.md` + `design/10-scope.md`. **Fixed:** (1) **de-jargoned the ask** — the card now leads with the plain act **"Steun vanaf €3 per maand"** ("you're just supporting"); the t-shirt is the token beneath ("draag je steun"); **"Word Kidi Buddy" dropped from the headline** (too specialised — it stays the Growfunding tier name only). (2) **Dropped the backer count** ("Al 18+ mensen steunen mee") — reassurance now via **participation scale** ("honderden gezinnen elke maand"), pulled live, never a hardcoded count (small number undercuts + stale-stats trap). (3) **Riding-stays-free reassurance added to *every* contextual block** (Home + event-detail), not just the page hero — they reach more first-timers, so the "meefietsen blijft gratis" clause can't live only on `/steun-ons`; added a copy rule to enforce it. **Left open for Frederik** (named in the critique, not auto-applied): the "Steun" vs "Meehelpen" nav-label ambiguity (#3) and the mobile prominence treatment for the 75%-mobile audience (#4). Still doc-only; no site code touched.

## [2026-06-02] build | Getting Started (P-12) — surface/UI pass, aligned to the ride/show page

Frederik: "verbeter de UI van /nl/getting-started" then, after a first gradient-based pass, "get closer to the layout decisions I made in the event/ride show page — let me guide the decisions." So I did a **thorough review of `activities/show.blade.php` + its CSS + the live render**, surfaced where my first pass diverged (gradient hero vs his solid-blue daisy hero; contained white sections vs full-bleed colour bands; light-yellow emoji chips vs red Flux-icon chips; borderless rows vs white tilted cards; a reinvented alignment ladder vs his real `.container`), and put the open calls to him via **AskUserQuestion**. His four decisions: (1) hero = **match the ride hero**; (2) "wat je mag verwachten" = **mirror the promises band**; (3) **full-bleed colour bands**; (4) **keep the accordion**. **Rebuilt to honour them by reusing his actual ride-page classes, not reinventing:** the hero reuses `.activity-hero*` (solid-blue full-bleed, `logo-icon` daisy bleeding right, circular illustration on `--light-blue`, sky **WELKOM** badge, -3° white headline) — GS-only adds are `.gs-hero__badge/__lead/__circle`; the expectations section reuses **`.activity-promises*` verbatim** (sky-blue band, big tilted H2 + `kid-on-scooter` illustration, white tilted cards with red Flux-icon chips), 3 cards/column with a scoped nth-child(3) tilt + the same IntersectionObserver stagger; the FAQ stays the `<details>` accordion (contained white, red +/× marker); the CTA became a **full-bleed yellow band** with a -3° ink heading + blue button. Colour rhythm now reads blue → sky → white → yellow → blue partners → ink footer (the warm gradient + emoji chips + contained panels from the first pass are gone). Copy + IA unchanged; appearance stays in `app.css` per the public-site rule. **Verified:** `npm run build` clean, hero/expectations/CTA + mobile screenshots reviewed (faithful match to the ride band; cards reveal on scroll), tests green (PublicStructureTest 21/46), Pint clean, 0 em-dashes. **Registry:** P-12 **UI 🔴→🟠** (Wire stays 🟠), row note rewritten to the ride-aligned description. **Wire/UI 🟢 still gated on Frederik's critique.**

## [2026-06-02] design | Support flow ("Steun Kidical Mass") — terminology + prominence rework

Ran strategy + UX planning (Garrett planes, Socratic) on the **#1 org goal: bring money in**, at Frederik's prompt that the footer "Word lid" CTA is wrong (*"je wordt geen lid — iedereen kan deelnemen; je steunt Kidical"*) and the goal is **under-represented** (footer-only). **Doc-only pass — no code touched** (Frederik's choice); a `/critique` follows. **Decisions (via AskUserQuestion, plane by plane):** (1) **Strategy — terminology:** public verb = **"steun" / "Steun Kidical Mass"**, giver "steunt mee"; **"lid"/member retired sitewide**; "Spacefunding"/"Kidi Buddy" survive only as model/entry-tier names. Page `/membership` → **`/steun-ons`** (route name kept `membership`). (2) **Strategy — prominence:** elevated from the deliberate *quiet-footer-only* call to **a "Steun" nav CTA + contextual blocks + footer** — **supersedes** the org-goals "keep it quiet" trade-off, bought back with warmth + a **riding-stays-free reassurance on every touchpoint** (a loud ask must not read as "pay to ride"). (3) **Strategy — primary fan:** a **family that just rode** (warm, high-trust). (4) **Scope — one-off:** recurring stays the lead, but a **discreet one-off path is provisionally back in scope** (reverses the v1 cut) → new concern **`D-9`, pending Leticia** + IBAN (Notion, not public repo). (5) **Scope — t-shirt:** reframed as a **visible token of support ("draag je steun")**, not "your membership". (6) **Structure — nav:** the "Steun" accent button **replaces the login button** top-right; **login moves to the footer** (already there). 5 main-nav links unchanged. (7) **Structure — placement:** contextual "Steun" blocks on **Home + end of event-detail** only (getting-started + chapter pages dropped); all asks → `/steun-ons`. (8) **Skeleton:** full `/steun-ons` ASCII mockup + the reusable two-variant contextual block. Stopped at Skeleton (no Surface). **Files:** `strategy/{10-organisation-goals,01-concerns}.md` (goal #1 + trade-off reworked; S-9 amended), `design/10-scope.md` (§ Support rewritten), `design/01-concerns.md` (**D-9** added, Partly; gate still 0-Open), `design/20-structure.md` (nav model + sitemap + key-decision + content-model), `design/40-patterns.md` (PAT-9/PAT-10 reworked), **renamed** `30-skeleton/membership.md` → **`steun-ons.md`** (rewritten), `30-skeleton/00-page-registry.md` (P-04 → "Steun Kidical Mass" `/steun-ons`; global-shell roll-up flags the planned-but-unbuilt rework), `glossary.md` ("steun" public, "lid" → don't-use). **Code untouched** (footer still renders "Word lid" → `/membership`; nav still shows login) — build work = terminology sweep + nav CTA + footer login move + the page + the contextual partial.

## [2026-06-02] build | J2 confident seam — Help-out picker + chapter sign-up form + critique fixes

Built the full J2 conversion seam end to end (Frederik: "build that, fix the wireframes, write proper copy"), after a `/critique` of the Help-out wireframe. **Critique fixes (Help out):** purged all em-dashes from rendered copy (added `assertDontSee('—')` guard), renamed "afdeling" → **"lokale groep"** (the duo's NL term), **resequenced** so the inspiring roles (not the obligations) are the last beat before the action, gathered the roles back at the CTA, restored the "honderden" scale signal. **The seam:** the CTA is no longer a button to the family `/chapters` index — it's an **inline group picker** (tap your group → `/chapters/[id]?intent=volunteer#aanmelden`); no map detour, no typing. Help out is now controller-backed (`VolunteerController` lists visible groups); route changed `Route::view` → `Route::get`. **Chapter form built:** replaced the `<x-wire.placeholder>` with a new **`ChapterVolunteerSignup`** Livewire component (name/email/**role checkboxes** incl. "Nog niet zeker"/message → `ContactForm`; warm "Je bent erbij" + next-ride `<x-event-card>` confirmation; `#aanmelden` anchor; `?intent=volunteer` welcome banner that leads with the form). **Deliberately a separate component** from the event-detail `VolunteerSignup` (shared with J1) — that one is untouched. `[backend remainder]` the enquiry emails the **central comms inbox tagged with the chapter name + roles**; true per-lead routing waits on a per-group lead email on the `Group` model (no schema change made). **Verified:** full suite **102 green** (+3 new `ChapterVolunteerSignupTest`: submit-tags-group-and-roles, validation, honeypot; +Help-out section/em-dash asserts), Pint clean, live render confirmed (picker = 8 group links w/ intent; chapter page shows form + welcome + 0 em-dashes; welcome absent without intent; event-detail still 200). **Fixed 2 pre-existing `BuildStatusTest` failures** (`pagesTotal`/count hardcoded 21 → 22) left stale by the concurrent Find-a-bike P-22 addition — not caused by this work. Registry P-11 + P-13 top-gaps updated; skeleton docs (`help-out.md`, `chapters.md`) note the build + the per-lead-routing remainder. **Still open:** per-lead routing (client/Nico), the chapter page's own **full NL pass** (it's still EN around the new NL form), is-the-ROI-public, UI/Surface pass + Frederik's Wire 🟢 critique.

## [2026-06-02] build | Footer + partners NL-localised, fat-footer rebuilt to IA, privacy/cookies folded

Frederik: the footer + partners block were still hardcoded English while the rest of the site is NL — fix that; then review the fat-footer **structure** against the new IA/scope/journeys; wire the social links (Instagram + Facebook only); fold the two legal pages into one. **Localisation:** moved all copy into NL lang files (new `lang/nl/footer.php`, `lang/nl/partners.php`; reused `nav.*`/`common.*`), wired `footer.blade.php` + `components/partners.blade.php` to `__()`. **Fat-footer rebuilt to mirror `20-structure.md`** (was a grab-bag: Home + Contact + Membership + Privacy + Cookies + Login in one column, plus three dead `href="#"` org links): now *Ontdek* = the 5-item main nav, *Over ons* = the About sub-nav, brand column carries the **persistent "Word lid" CTA → /membership** (IA: membership is a persistent footer CTA + top org objective), and a utilities bottom bar (Contact · Privacy & cookies · Inloggen). Dropped off-strategy **"Donate"** (one-off donation is dropped for v1 — support is recurring membership) and the duplicate `mailto:` Contact. **Social:** reduced to Instagram + Facebook (LinkedIn removed), wired to the live accounts (IG `kidicalmass.belgium`, FB `Kidicalmass.brussels`, confirmed by Frederik). New `.site-footer__cta` (yellow pill) in `app.css`. **Legal fold:** privacy + cookies are now **one page** — `/privacy` titled "Privacy & cookies" with both sections; `/cookies` 301s to it (preserves any links indexed from the old Wix site); `cookies.blade.php` removed. **Verified:** `npm run build` clean, footer screenshot reviewed (all NL, 2 socials, single legal link), tests green (NavigationTest updated: footer asserts one legal link + a /cookies→/privacy 301 test; PublicStructureTest drops `/nl/cookies` from the 200 sweep), Pint clean. **Docs:** `20-structure.md` (footer cluster + sitemap + key-decision lines reflect the fold), `00-page-registry.md` (P-06 gap note = folded + still stub; roll-up notes the legal fold + a "global shell" line for the footer/partners work — the footer is shell chrome, not a P-row, so no page-stage emoji flips). **Still open:** legal text itself is still a stub (P-06 unchanged 🔴/❓); FR mirrors (`lang/fr/footer.php`, `lang/fr/partners.php`) needed when the FR layer lands; the yellow CTA styling is Frederik's to critique.

## [2026-06-02] build | Em-dash purge + tone-of-voice rule; police wording confirmed

Frederik: the copy had too many em-dashes, "a clear tell of AI-generated content" — fix the copy and add the rule to the tone-of-voice doc. **Copy:** de-dashed all rendered NL copy on Getting Started (6) and Find a bike (5), rewriting each into natural Dutch (full stop for two thoughts, comma for an aside, colon to introduce) rather than a mechanical swap. Verified 0 em-dashes in the live HTML of both pages. **Tone of voice:** added an em-dash rule to `docs/tone-of-voice.md` "What to Avoid" (banned as a connector in all interface copy, every language; compound hyphens fine), and de-dashed the doc's own prose + examples to practice it — the four voice-quality headings now use the parenthetical form already used in CLAUDE.md ("Joyful (not frivolous)"). Only the rule line keeps an em-dash, referentially. **Police wording CONFIRMED** (Frederik): resolved P-12 open-Q #9 and the content-companion safety-FAQ note from 🚩 to ✅ — "waar nodig stemmen de organisatoren de route af met de lokale politie" stands. **find-a-bike slug** confirmed (no change). No registry stage changes (copy refinement, not a stage bump). Tests green (22), Pint clean. Blade `{{-- comments --}}` still contain em-dashes (dev notes, not rendered) — left as-is.

## [2026-06-02] build | Help out (P-13) — NL orientation wireframe (J2 reframe)

Built the **Help out** page (`resources/views/volunteer.blade.php`, NL, wireframe fidelity) from the just-reframed [`help-out.md`](design/30-skeleton/help-out.md), at Frederik's request to "move further + get NL wireframes." Was a 2-line stub ("Meer info volgt binnenkort"). Now the **J2 orientation page**: header → pitch (barrier-dissolver "je hoeft geen fietsexpert te zijn") → 5 role cards as invitations (roze hesje · mede-organisator · communicator · fotograaf · DJ, emoji icons like Getting Started) → "Wat meedoen inhoudt" honest get/ask (distilled from the ROI principles; "vrijwilligersafspraken" links the guidelines doc at the *commitment* moment) → **single primary CTA "Vind je afdeling" → /chapters** → calm "nog geen afdeling?" secondary exit (mailto + chapters link). **No contact form** — the reframe routes via the chapter; the form lives on P-11. Mirrors the Getting Started conventions exactly (`<x-layouts::site>`, raw headings, semantic HTML, structural classes only, Surface deferred). **Verified:** live render at `/nl/help-out` (curl confirms all sections; old stub gone), `PublicStructureTest` +1 test (22 passing, asserts the sections + that the page routes to `groups.index`), Pint clean. **Registry:** P-13 **Wire 🔴→🟠**, **Back ❓→⚪** (page is now static — the routed form + backend live on P-11), top-gaps + roll-up updated. **No screenshot** (playwright unavailable in this env) — visual/aesthetic check deferred to the Surface pass + Frederik critique (UI 🔴). **Chapter-page form (P-11) deliberately not touched** — it's an honest `<x-wire.placeholder>` on an already-rebuilt, model-backed, shared-tree page; turning it into the real role-checkbox form is a Back task (needs the `Volunteer enquiry` model + routing), proposed as the next step rather than faking a non-functional form.

## [2026-06-02] build | Find a bike (P-22) — no-bike detail split off Getting Started

Frederik: the "Geen fiets?" block "feels odd" sitting on Getting Started even after the distill — *"fold a short version into the FAQ and give it its own page."* Agreed (a rental-shop list is reference material, not conversion material). Scope chosen (AskUserQuestion): **no-bike providers only** — the earlier-relocated "Andere manieren" stays parked for a later/different home, not merged here. Built **new page `find-a-bike.blade.php`** (`/find-a-bike`, route name `find-a-bike`, NL, wireframe fidelity): Kidical Mouse, Loopz, Fietsbieb, My Kids Bikes + Cyclo note + a "een fiets gevonden? → /events" CTA and a back-link. **Standalone, deliberately *not* in the locked 5-item nav** — reached from the Getting Started FAQ, reusable later from event/chapter pages. On Getting Started the section is **gone**; a single FAQ item ("Wat als we geen fiets hebben?") keeps the brand promise on-page and links out. Registry: **new row P-22** (UX 🟢, Conf 4, Wire 🟠, UI 🔴), P-12 top-gaps updated, roll-up 21→22 rows / 16→17 UX 🟢. Tests: added `/nl/find-a-bike` to the 200 sweep + a content test, and split the moved assertions (KIDICALMASS/€30 now asserted on find-a-bike, `assertDontSee('KIDICALMASS')` + the FAQ-link assertion on Getting Started) — both-way leak checks. All green, Pint clean, dashboard parses 22 rows with no warnings/drift. Slug `find-a-bike` is easy to rename. `[client]` Kidical Mouse availability still open.

## [2026-06-02] design | J2 "become a volunteer" flow — folded into design/ + ux-folder migrated

UX pass on **journey J2** (potential volunteer → routed local contact) at Frederik's request, picked as a flow that doesn't collide with the parallel J1 "newcomer → first ride" thread (J2 owns Help out + the chapter-page form; J1 owns Home/Events/Event detail). Worked the planes top-down via AskUserQuestion. **Core reframe (Frederik's choices):** routing is **wayfinding through the chapter**, not a form field — so **Help out becomes a pure orientation page** and the **contact form lives on the chapter page** (routed by context). This *reconciles* the stale `help-out.md` (which still put a form + municipality dropdown on Help out) with the already-decided `20-structure.md` ("the volunteer contact form lives on chapter pages") and `chapters.md`. **Decisions:** (1) confirmation promises **"the team", no name/SLA** — deliberately independent of the unvalidated D-1 reply-loop assumption; (2) the **post-submit limbo** is answered with the chapter's **next-ride hook** (reuses ride data already above the form); (3) **role checkboxes** (incl. "Not sure yet") carry onto the chapter-page form (matches the `Volunteer enquiry` entity); (4) "start a chapter" stays a calm **secondary exit** (anti-overlap, avoids J3 territory). **ROI/guidelines repurposing:** the charter is organiser/escort (P4/P5) operational material — only the **🧭 principles** are repurposed (kindness, inclusion, safety) + **"Action positive" becomes a tone constraint for the whole flow** ("positive, not preachy"); the full doc is a **commitment-stage external link** renamed "volunteer guidelines", not an entry gate; operational detail → onboarding/back-office (D-1). **Files:** rewrote `help-out.md` (orientation reframe, J2 flow diagram, guidelines table, open Qs), updated `chapters.md` (form role checkboxes + resolved confirmation state — closes its open-Q #4), `help-out-content.md` (form copy relocated, guidelines moved to commitment, role copy kept), `20-structure.md` (Help out sitemap node). No new `01-concerns` entry — decisions are made; client/build remainders live in the page Open-Questions sections (preserves the 0-Open gate). **ROI captured** to `docs/raw/website/volunteer-roi-charter.md` (FR + NL verbatim) with the **named-signatory list redacted** per the public-repo guardrail — ⚠️ note the [2026-06-02 P-12 entry below] recorded "ROI not committed to the public repo"; this save is **working-tree only, signatories redacted**, pending Frederik's call to keep or pull. **Migrated + removed the orphan `docs/ux/` folder** — its content was already in the Cascade wiki and in places stale (e.g. "NL/FR/EN all in scope" vs decided **D6** NL+FR): `activity-detail/` was already fully in `30-skeleton/activity-detail.md` (newer, with the PAT-18 note); root `_strategy.md`/`_scope.md` superseded by `strategy/20-personas`, `strategy/10-organisation-goals`, `strategy/00-strategy-brief` (D1/D3/D6), `design/10-scope`. No registry stage bumps (this is re-planning, not a build).

## [2026-06-02] build | Getting Started (P-12) — safety reassurance from the volunteer ROI + Jorge interview

Frederik reviewed the volunteer **Règlement d'Ordre Intérieur** (organiser/accompagnateur charter) for first-timer-relevant content. Verdict: it's J2/J3 material (volunteer/organiser), **not** J1 — ~90% belongs to Help-out (P-13) / back-office (P-09) / Mission values (P-15), and dumping it here would undo the distill. But **safety is the first-timer's top anxiety**, so two concrete, public-safe facts were surfaced surgically (no new sections beyond one FAQ): (1) the "trained volunteers" fact card became **"Minstens vier roze hesjes"** — front/back + every junction clear (ROI: min. 4 marshals, ~1 per 10 riders, capitaine-ouvreur + vélo-balais); (2) a new FAQ **"Is het veilig in het verkeer?"** carries the **police-coordination** reassurance Frederik recalled from the **Jorge interview** — phrased "waar nodig stemmen de organisatoren de route af met de lokale politie" (matches ROI's *"coordonner le parcours avec les autorités locales si besoin"*). Pace number (8–9 km/h) deliberately **not** added — qualitative "kindertempo" reassures better than a number a non-cyclist will second-guess. **Guardrail:** the ROI carries named signatories + governance → **not committed to the public repo, not saved to Notion** (per Frederik); only the generic safety facts surface publicly. New open question logged (P-12 #9): confirm public police wording with Frederik/Leticia — involvement varies by commune. Updated `getting-started.blade.php`, content companion (card 6 + new FAQ, EN/FR/NL), skeleton open questions, test (`assertSee('lokale politie')`). Tests green, Pint clean. **Still to route:** ROI is the source charter for the J2/J3 volunteer/organiser builds — pointer not yet dropped in those briefs.

## [2026-06-02] build | Getting Started (P-12) — distilled (Frederik critique)

`/critique` on the built page (Frederik): *"a ton of content sits between the intention and the CTA; 'Andere manieren' doesn't belong here, and 'Geen fiets?' neither."* Diagnosis: the page was doing three jobs (reassure a first-timer / solve a no-bike barrier / be a cycling-resource hub) and burying its one CTA under the latter two. Applied **`/distill`**: (1) **CTA "Vind je rit" lifted** to directly after the FAQ — the natural "yes" moment; (2) **"Andere manieren om te fietsen" removed** from the page (it sent first-timers *away* at the decision point — relocated to the content file for a future resources/chapter spot); (3) **"Geen fiets?" demoted below the CTA** and slimmed from a 4-card grid to a plain list (brand promise kept, off the critical path). Live order now verified: CTA precedes the no-bike block; "Andere manieren" absent. New flow = reassure → answer → **go**, edge case as a footnote. Updated `getting-started.blade.php`, the skeleton Structure + Scope notes, the content file (relocation note), test (added `assertDontSee('Andere manieren')`), and P-12 top-gaps. Tests green, Pint clean. Wire/UI 🟢 still pending the Surface pass. Visual/AI-slop audit deferred — no surface yet.

## [2026-06-02] build | Getting Started (P-12) — first static page off the stub list

Ran the **newcomer → first ride** flow (J1 entry, flow A) at Frederik's request ("UX planning + wireframing for one user-story flow"). Audit found the flow's planning already complete — `getting-started.md` is ✅ with full desktop+mobile skeletons + trilingual content — but the **view was a bare `<x-stub>`**, and the Home hero's "New here? Start here →" pointed at `#upcoming`, **bypassing the page entirely**. Two fixes shipped: repointed the Home entry link to `route('getting-started')`, and **built the NL wireframe** (`resources/views/getting-started.blade.php`) from the skeleton — header, 6 what-to-expect fact cards, 6-item FAQ, "Geen fiets?" (4 providers), "Other ways to cycle" (3), CTA → /events. Wireframe fidelity (semantic HTML, structural layout only; Surface/UI pass deferred). **Re-plan caught stale facts** (re-verified, sources in new `getting-started-research.md`): **Fietsbieb** changed 1 Jan 2026 → **€30/yr (€10 reduced) + €20 deposit, now Vlaanderen+Brussel** (was "€20/yr, Brussels-only"); **Loopz** domain loopz.be→**loopz.bike**; the content file's **"helmet required under 12" note was factually wrong** (no such Belgian law) and removed; My Kids Bikes + Pro Velo Families-on-Bike confirmed active (locations softened). **Open:** Kidical Mouse ride-availability (web-unverifiable → client). **Scope flag:** the "don't have a bike" provider block was deferred out of v1 core on 2026-06-02 pending verification — verification now done, so it's built and ready; ship-in-v1 is Frederik's call. Tests: repointed the stub-assertion to `/nl/about/mission`, added Getting Started section coverage + the Home entry-link assertion (PublicStructureTest, all green). **P-12** Wire 🔴→🟠, Conf 3→4, Assets 🔴→⚪ in `00-page-registry` (+ roll-up); dashboard parses clean. Wire/UI 🟢 still gated on Frederik's critique + the UI pass.

## [2026-06-02] design | Content cross-check — old Wix bodies vs new structure

Cross-checked the new structure against the raw Wix scrape (`docs/raw/website/`) at Frederik's request ("make sure we didn't lose anything; are there content gaps?"). **Page-level: complete** — every old page already has a destination in [`25-content-migration`](design/25-content-migration.md); nothing whole is lost. Reading page *bodies* surfaced content-fragment risks the title-map hides, now logged in `25-content-migration` § Content-level cross-check. **Biggest risk:** chapter pages launch near-empty — only **Namur (/5000)** + **Mons (/7000)** have real scraped content; the ~13 Brussels postal pages aren't in the scrape (audit confirms "not one has its own page"), so cutover needs *content authoring* by leads, not a data move. **Structural fixes applied** to `20-structure`: chapter template gained an **intro / "what our rides are like"** block + an optional **History / our story** block (both used by the real Mons/Namur pages, both missing from the template). **Three decisions (Frederik):** (1) **Liège + Mons both hosted as full chapters** — *reverses the locked "Liège = external pin" call* (updated `20-structure` key decisions + chapters node, `25-content-migration` cross-check + Decision Flag #5); (2) **one-off bank donation (BE72…) dropped for v1** (recurring-only stands; confirm with Leticia); (3) **"don't have a bike" provider section deferred** out of v1 core (provider list — Cyclo/Fietsbieb/Loopz/My Kids Bikes vs our "Kidical Mouse" — to verify later). Other fragments flagged to carry: the canonical "what is a ride" spec (no draisiennes, accompanied child, 5–7 km, free) → Getting Started + event defaults; Vision needs rewrite + fresh NL (FR-only, dated 2024-elections, Brussels coalition); volunteer ROI doc + Safety-First video → back-office (D-1); national funders + #StreetsForKids affiliation → Partners; contact `.be`/`.brussels` domain split to resolve at build.

## [2026-06-02] design | Structure sweep — sitemap validated + logged-in IA locked

Full Structure-plane (plane 3) review at Frederik's request ("get the sitemap right; public + logged-in"). **Coverage check:** traced every [persona](strategy/20-personas.md)/[JTBD](strategy/30-jobs-to-be-done.md)/[journey](strategy/50-user-journeys.md) to the canonical sitemap — public IA holds, no scope creep. **Three gaps caught + homed (Frederik):** (1) **per-region email subscription** (a Scope feature) had no home → control on Events + chapter pages; (2) **legal/utility pages missing** → `/privacy` + `/cookies` (GDPR — enquiry form + email opt-in) + 404 in a footer cluster; (3) **no national contact front door** → new `/contact` → coordination duo (press/partnership/general buckets), distinct from chapter-routed Help out. **Logged-in IA locked (3 forks):** (a) **back-office = separate branded frontend surface** (`/backstage/[postal-code]`, read-mostly), **not** Filament — volunteers (P4) never touch `/admin`; (b) **accounts invite-only** — leads provision in `/admin`, **no public Register**, login link only, post-login landing = My activities; (c) recorded as a **three-tier** model (public / logged-in frontend / admin CMS). Cascaded across `design/20-structure` (nav + footer, full three-tier sitemap, 6 new key decisions, 2 new content-model entities — Contact message + Email subscription), `design/30-skeleton/00-page-registry` (5 new route rows), `design/01-concerns` (D-1 boundary decided; content detail still pending Alexandre). **Deleted** the stale `docs/ux/_structure.md` (a pre-Cascade duplicate that still said "Contribute"/News-as-nav/3 languages/no logged-in pages). Remainder: back-office *content* brief still waits on the Alexandre/J3 interview (D-1).

## [2026-06-02] design | D-4 — surface tokens closed (DESIGN.md documents app.css)

Item 4 of the design review walk. **Finding:** the D-4 premise ("no machine-readable tokens; brand in prose only") was outdated — a full Tailwind v4 `@theme` token set is **live in `resources/css/app.css`**, set in the merged `design/activity-page` redesign (verified: branch is fully contained in main, app.css identical). **Decisions (Frederik):** (1) **`app.css` is canonical** (where Tailwind reads tokens); a new **`DESIGN.md`** at repo root documents it (no duplicated hex → no drift); (2) the as-built palette + type is the **approved** brand system (follow the redesign branch). Wrote **`DESIGN.md`** — palette (ink `#281a39`, yellow `#f9d924`, blue `#1d67cd`, + red/green/orange/sky/light-blue/light-yellow, zinc neutrals), semantic tokens, typography (**Poppins** headings / **Nunito Sans** body / **Fredoka One** logo), and the signature design language (−3° tilts, red rounded-square icon chips, full-bleed colour blocks, yellow→orange gradients, animated yellow link underline, oklab color-mix tints). **De-staled `50-surface.md`** (was "dark blue+white+yellow" + TBD sketch → corrected palette/type, points to DESIGN.md as built). `D-4` **Open → Closed**. Updated `40-patterns` gap note, `00-design-plan` Surface row (🟡→✅), `01-concerns` (now **0 Open / 3 Partly / 2 Closed** — no fully-Open design concern remains), and `index.md`.

## [2026-06-02] design | D-7 — redirect map drafted (Open → Partly)

Item 3 of the design review walk (D-1 parked pending Alexandre). Created **`design/26-redirect-map.md`** — every old Wix URL → new route, all `301`, derived from the migration plan + site-audit inventory. **Language rule decided (Frederik):** redirects target **neutral paths**; a **locale middleware** resolves `/nl/`|`/fr/` via Accept-Language → cookie → geo, **fallback NL** — so the map stays language-neutral. Proposed Drop-targets confirmed: gallery → `/`, downloads → `/about`, shop (`/product-page/*`, `/category/all-products`) → `/membership`. Unmapped legacy paths → 404 (no masking). `D-7` moved **Open → Partly** — design side resolved; remainder is build-only (middleware + 301 config, fill Grande KM event `{slug}`s at seed, confirm the two combined-postal canonicals, preserve `/post/{slug}` slugs, post-launch crawl of the critical set `/agenda` + postal pages). Design register now **1 Open (`D-4`) / 3 Partly (`D-1`,`D-3`,`D-7`) / 1 Closed (`D-2`)**. Updated `25-content-migration` (redirect open-question → resolved pointer) and `index.md`.

## [2026-06-02] design | D-2 review — closed (meetups public, chapter pages only)

Design-phase review pass, item 1 of the walk (D-2 → D-1 → D-7 → D-4 → verification → bookkeeping). Settled the three open sub-questions on meetup visibility (Frederik): **(1) status — treat as locked** (no further client gate, accepted as diverging from Leticia's "strong local communities" stance); **(2) detail level — fully public, all details** incl. meeting point; **(3) where surfaced — chapter pages only** (no national movement/aggregation view, not on the `/events` ride calendar; a logged-in volunteer's cross-group view stays in My activities). `D-2` moved **Partly → Closed** (design register now 2 Open / 2 Partly / 1 Closed). **Fixed a stale drift:** `20-structure` sitemap still showed "logged-in account also sees meeting/workshop activities" on `/events` (pre-revision model) — removed; `/events` is rides-only. Cascaded across `design/01-concerns` (D-2 → Closed + at-a-glance/gate), `design/10-scope` (MVP core, viewing rule), `design/20-structure` (sitemap + key decisions), `my-activities` brief, and the `strategy/01-concerns` S-4→D-2 lineage. `strategy/00-strategy-brief` D8 already encoded public viewing — unchanged.

## [2026-06-02] strategy | Locked 3 Leticia-grade signals + movement-facts block

From the signal-vs-strategy walkthrough, locked (Frederik): **(1) "the site mirrors the org's governance"** (local → national) — added as a design principle; **(2) attendance display = hosts/organisers only, not the full attendee list** (Leticia) — applied to D-1, scope, PAT-18, and the Attendance content-model row; **(3) funding rationale "subsidies won't always flow"** — added to org-goal #1. Added a compact **Movement facts** block to `10-organisation-goals` (~50 events/season, ~300 active, ~15–20 groups [13 on Growfunding], ~500 mailing list, ~75% mobile). **Declined:** "Local > mass" (not locked). **Held as signal for Alexandre:** volunteer role ambiguity + post-onboarding pain (feed D-1).

## [2026-06-02] decision | Meetups are public (revises D-8/D-9)

Frederik decided meetups/workshops are **publicly viewable** (not logged-in-only) as a traction/recruitment signal — resolving the open `D-2` sub-question. This **revises `D-8`/`D-9`:** login no longer gates *viewing*; it gates **attendance + the back-office** only. Cascaded across `strategy/00-strategy-brief` (D-8/D-9), `design/01-concerns` (D-2 → decided: public, cross-group), `design/10-scope` (viewing rule, accounts, build implication), `design/20-structure` (content model, group_user, sitemap, key decisions — dropped the meetup view-gate field), `glossary` (meetup + group_user rows), and the `my-activities` brief. Remaining design detail: *where* public meetups surface (chapter pages + a movement view, kept out of the family ride calendar) and the default-municipality filter (to test).

## [2026-06-02] strategy | Review pass — fact-checked vs primary sources, tightened, D-1/D-2 decided

Reviewed all 7 strategy pages against the primary sources (Leticia meeting + the two completed volunteer interviews, Jorge/Woluwe & Morgane/Auderghem). **Guardrail applied (per user): interviews are signal, not strategy** — reframed interview-derived assertions as unvalidated signal in `20-personas` (P4) and `50-user-journeys` (J2); added a note to `01-concerns`. **Fixes:** corrected the chapter count (`~45` → `~15–20`; 13 on the Growfunding page); "Five"→"Six" actors; **compressed `30-jobs-to-be-done` to a table** (was the heaviest/least-unique doc); removed the "no bike? no problem" subhead detail from `40-value-proposition`; source-hygiene across files (dead `wiki/service-design`, `wiki/strategy-plan`, `wiki/ux-planning` refs → `notion`/new locations); updated J3 evidence note (both interviews are pink-vests → J3/chapter-lead still untested, Alexandre pending) and fixed the `S-3`→`D-1` pointer. **Strategic calls recorded in the Design register (canonical):** `D-2` **resolved cross-group** — any logged-in volunteer sees all groups' meetups (traction + recruitment rationale; default-filter to own municipality, to test; open sub-question: should meetup existence be *public* for non-logged-in visitors — would revisit `D-8`). `D-1` **validated as a design input** — a two-layer per-chapter volunteer back-office (what-to-expect pre-signup; how-it-works/docs/video/meetup-schedule/chapter-leads-and-roles post-login), detail pending the Alexandre interview (may request his material). Updated `design/01-concerns` (D-1/D-2 Open→Partly), `design/10-scope`, and the `my-activities` brief.

## [2026-06-02] tidy | Convention cleanup (phase frontmatter, voice dial, key-decisions fold)

Cleared the outstanding tidy items. **Phase frontmatter:** added `phase:` to the 19 legacy files that lacked it (site-audit → discovery; tone-of-voice → cross-cutting; all `design/30-skeleton/*` + the two asset maps → design; key-decisions → strategy) via a deterministic script. **Voice gate:** added a **Register dial** (joyful/warm ←→ serious/factual, hard stops at corporate + militant) and a **Pre-ship checklist** to `tone-of-voice.md`, so the glossary's forward-reference to "register dial + pre-ship checklist" now resolves; fixed the "espacefunding" → "spacefunding" typo. **key-decisions fold:** `git mv key-decisions.md → strategy/90-key-decisions-evidence.md` (90 = evidence per Cascade numbering), reframed its header to mark the [Strategy Brief](strategy/00-strategy-brief.md) canonical and itself as underlying evidence; updated the `index.md` row and the two strategy frontmatter `sources` pointers. Link check clean.

## [2026-06-02] gaps | Resolved 3 gap clusters via interview (mission/VP, back-office/visibility, missing briefs)

Worked the prioritised gap list with the user. **Mission/VP (S-1/S-2):** locked working versions (Leticia sign-off still pending, so kept Partly) — hero leads on joy/scale ("Join thousands of families taking over the streets…") + inclusivity subhead; internal mission is the 4-job statement (find · participate · run · **fund**, the funding clause added per user). **Back-office/visibility (recorded as provisional leans, pending Alexandre/J3 + Leticia):** `D-1` → minimal per-chapter back-office **+ attendance**, provisionally IN scope (was a not-settled cut); `D-2` → cross-group meetup visibility. **New cross-cutting rule:** "I'm coming" attendance is **account-only and volunteers-only**, on **all** activity types (rides + meetups) — a volunteer roster, not a family headcount — adding an **Attendance** entity (volunteer↔activity). **Accounts clarified: volunteers only** (families have none; spacefunding members pay externally, no site account). **Two missing briefs written:** `30-skeleton/membership.md` and `30-skeleton/my-activities.md`. **Growfunding reality check:** fetched the live campaign — platform is **Growfunding** (not "Growthfunding"), product is **Spacefunding**, recurring only (monthly/annual), 6 tiers €3–€500/mo, entry €3 "Kidi Buddy" = t-shirt, €20+ tiers grant logo placement; membership page **links out**, no on-site payment. Updated `glossary.md`, `strategy/{01-concerns,40-value-proposition}.md`, `design/{01-concerns,10-scope,20-structure,40-patterns}.md`, the page registry, and reconciled `activity-detail.md` (volunteer attendance moved from "deferred" to provisionally IN; family RSVP still out).

## [2026-06-02] glossary | Settled the public word for a ride (parade vs rit)

Resolved the long-standing NL wording question. Swept the full Wix scrape and the new-site drafts for every candidate term. Finding: the coordinators' own published copy is consistent — **parade / fietsparade** *defines* the thing on every mission/about/stats page (`le-projet-het-project.md`: "organiseert feestelijke **fietsparades**"; homepage: "feestelijke, veilige **fietsparades** op kindermaat"), with **balade / fietstocht** as the warm in-motion word and FR **cortège** for the escorted procession. **`stoet` appears zero times anywhere** (and reads as carnaval/begrafenis) — the old glossary value "stoet / rit" was ungrounded. `rit` was only incidental in coordinator copy (1×, a DJ blurb); it had crept into the new-site CTA drafts as Frederik's choice. **Decision (Frederik):** NL public = **fietsparade** (concept) · **fietstocht / tocht** (in motion); drop **stoet, rit, optocht**. FR = **parade cycliste festive** · **balade** · **cortège**. EN internal "ride" unchanged. Updated `glossary.md`; sweeping `rit`/`optocht` → `fietstocht`/`fietsparade` across the NL `design/30-skeleton/*-content.md` + `*.md` drafts.

## [2026-06-01] restructure | Cascade Design phase re-shelved (planes)

Re-shelved the Design phase onto Garrett's planes. **Moved (git mv, history preserved):** `ux/` → `design/30-skeleton/` (7 specs + 7 content companions); `image-map.md` → `design/60-asset-map.md`; `image-map-new-site.md` → `design/61-asset-slots.md`. **Split** `ux-planning.md` (deterministic script) into `design/10-scope.md` (plane 2), `design/20-structure.md` (plane 3 — nav, sitemap, **+ new content-model table**), `design/25-content-migration.md`; its Strategy section was dropped (superseded by the Strategy phase) and its Principles folded into `design/00-design-plan.md`; `ux-planning.md` removed. **New artifacts:** `design/40-patterns.md` (PAT-1…PAT-17 with stable IDs), `design/50-surface.md` (visual direction; flags that `DESIGN.md` tokens don't exist yet), `design/30-skeleton/00-page-registry.md` (per-route spec/content/build/lifecycle tracker — build status marked unverified pending a pass with Nico), `design/01-concerns.md`. **Lint/reconciliation:** strategy concerns `S-3`/`S-4`/`S-5` **graduated** to design `D-1`/`D-2`/`D-3` (canonical there) now that Design is open; strategy register updated (0 Open, 2 Partly, 5 Closed, 3 Graduated). Fixed all internal links (depth changes in moved skeleton files, renamed asset-map cross-refs, strategy→design pointers) and rebuilt the `index.md` Design section. Surface tokens (`D-4`) and the Membership / My-activities briefs remain the notable design gaps.

## [2026-06-01] restructure | Adopt Cascade structure — Phase 0 scaffold + Strategy phase

Began migrating the wiki onto the **Cascade** playbook (phases Discovery → Strategy → Design → Build; planes within Design). **Phase 0 (scaffold):** created `discovery/`, `strategy/`, `design/`, `build/`, `_archive/`; added `glossary.md` (NL/FR vocabulary + don't-use terms, flags the spacefunding/Growthfunding ambiguity); added phase plan/status stubs to discovery (pointer — discovery stays in Notion, out of git per user), design (maps where each plane currently lives), and build (YAGNI, not opened); documented the Cascade schema + page conventions in `CLAUDE.md`. **Strategy phase (built out):** `00-strategy-brief.md` (locked decisions D1–D9 distilled from key-decisions + ux-planning + the Leticia interview), `01-concerns.md` (keystone register — 2 Open / 3 Partly / 5 Closed, stable `S-n` IDs), `10-organisation-goals.md` (ranked, money-in #1), `20-personas.md` (5 actors + anti-overlap contract), `30-jobs-to-be-done.md` (new), `40-value-proposition.md` (new — overall + per-persona, S-1/S-2 drafts), `50-user-journeys.md` (J1–J3, J3 evidence gap flagged). Rebuilt `index.md` with a Phase column. **Deliberately skipped:** Discovery re-shelving (kept in Notion) and Design/Build content re-shelving (a later pass). Sources read: key-decisions.md, ux-planning.md, and the deleted-but-in-HEAD service-design.md + strategy-plan.md (recovered read-only, not re-committed).

## [2026-05-18] ux-planning | Internal meetups / activity types folded into scope

Gap found: the interview revealed Leticia's internal meetups + that Nico already built an `Activity` model (types `kidicalmass`/`meeting`/`workshop`/`other`, bilingual NL/FR, group-linked) with a separate `Article` model and a `group_user` pivot — none of which was in ux-planning (Events only ever covered public rides). Verified on `origin/main`; no visibility field exists yet. Applied 3 user-confirmed decisions: (1) non-ride activities visible to any logged-in account across all groups; (2) one Events system gated by type (public rides + account-visible meetups, chapter page + a "My activities" account view); (3) keep "group-volunteer account" distinct from "spacefunding paying member." Updated Scope (Core, new Activities + Accounts functional blocks, Content Requirements), Structure (sitemap + Key Structural Decisions), and added a build implication for Nico (needs a visibility rule) + a "validate with Leticia" open question (cross-group visibility is broader than her local-communities stance).

## [2026-05-18] ux-planning | Strategy & scope updated from Leticia interview

Applied four user-confirmed decisions + several confirmations from the interview. Strategy: added "bring money in" as the top organisational objective (recurring membership/spacefunding, site-wide); demoted "potential chapter leads" from primary to secondary audience and reframed growth toward participation in existing chapters; resolved the Facebook-vs-site open question (site is canonical, FB stays for reach/turnout); added a light-and-broad positioning guardrail; logged the private organiser back-office + "who's coming" as an explicit OPEN question (deferred to post-interview). Scope: v1 is bilingual NL+FR (English deferred); added a Membership/spacefunding page + global footer CTA; confirmed cuts (web store, photo-gallery system, poster/flyer generator, worked-out chapter-start flow); low-frequency per-chapter subscriptions; clearer volunteer onboarding. Structure: sitemap + language routing updated, `/membership` node added, Key Structural Decisions revised, Decision Flags reconciled (4 of 5 resolved; Grande Kidical still open).

## [2026-05-18] ingest | Leticia interview — structured transcript

Ran the Leticia site-strategy interview (2026-05-18). Synthesised the raw auto-transcription into a structured, theme-organised document at `docs/raw/interview-leticia-2026-05-18.md` (governance, scale, funding, audiences, events workflow, subscriptions, scope in/out, the contested organiser back-office, ecosystem/politics, next interviews, look & feel). Key new signal: funding/membership is a top org goal absent from Strategy; "potential chapter lead" as a primary audience is overstated (challenge is participants, not groups); Facebook stays as a turnout signal; trilingual → bilingual NL/FR for v1. Evaluation against `ux-planning.md` (strategy + scope) pending user confirmations.

## [2026-05-18] ux-planning | Content Migration Plan — page-by-page table + meeting visual

Completed the previously-stubbed Content Migration Plan in `ux-planning.md`. Added the full page-by-page mapping (every current Wix page → action per the Rewrite/Migrate/Merge/Absorb/Drop/Seed legend → new home), grouped into National & content, Events, Chapters, and News/Press/Store, sourced from `site-audit.md` and the new sitemap. Added a "Decision Flags for Leticia" subsection surfacing the five non-technical choices the new structure makes (web store, photo gallery, "no bike" demotion, Grande Kidical normalisation, Liège external). Produced a derived standalone meeting artifact at `docs/site-structure-comparison.html` — a self-contained side-by-side visual (current vs new sitemap, colour-coded actions, decision flags) for the site-strategy review with Leticia; kept separate from the wiki internals. Removed the redundant `docs/raw/current-live-site-kidicalmass-be.md` (superseded by the existing `docs/raw/website/` scrape + `site-audit.md`).

## [2026-04-13] image-map | Photography expanded — Downloads batch

Added 23 new photos from `~/Downloads/kidical-photos/`. Identified 4 duplicates: upgraded `ride-brussels-boulevard-crowd.jpg` (787→1960px wide) and `volunteer-pink-vest.jpg` (740→1200px tall) with higher-res originals; skipped `cf0153_6f0e0e6d` and `cf0153_0a3acc1b` as duplicates of `kidical-mass-16` and `kidical-mass-13`. Photography collection now 36 photos. Highlights: three 8688×5792 professional shots, Cinquantenaire crowd at 5472×3648, DJ-on-boombox-bike. Homepage and About hero slots upgraded from ⚠️ to ✅. Updated `image-map.md` and `image-map-new-site.md`.

## [2026-04-13] image-map | Asset catalogue + new site slot map

Visually assessed all 143 files in `docs/raw/website/assets/`. Applied exclusion criteria from spec (size, screenshots, iStock, social story formats, Wix UI elements, QR codes, third-party event assets). Copied 50 usable assets to `docs/raw/assets/` with clean kebab-case names: 11 illustrations, 13 photos, 11 event posters, 12 chapter posters, 3 logos. Wrote `image-map.md` (asset catalogue) and `image-map-new-site.md` (per-page slot map with gap priority list).

## [2026-04-13] ingest | Notion project — initial migration

Migrated 7 pages from the Notion project "Kidical Mass Belgium — Website Project" verbatim:
desk-research, service-design, key-decisions, tone-of-voice-notion, strategy-plan, site-audit, ux-planning (with 14 nested child pages).
Excluded: Meeting Notes (top-level template only), Look & feel examples.

## [2026-04-13] ingest | Existing codebase docs — initial migration

Migrated docs/ux/ (strategy, scope, structure, activity-detail sub-pages) and docs/tone-of-voice.md into the wiki verbatim.

## [2026-04-13] ux-planning | Full page-level UX specs — all remaining pages

Completed thorough UX planning for all remaining pages, matching the depth of the Activity Detail spec. Each page now has: deepened Strategy (psychological depth, user mental states), confirmed Scope, confirmed Structure, ASCII wireframes (desktop + mobile with annotations), and an Open Questions / Necessary Refinements section.

Pages completed:
- **Home** — hero structure, events strip, chapter map, stats, volunteer CTA, news, partners bar
- **Events Overview** — filter bar, date-grouped list (upcoming + past modes), Grande KM badge
- **Getting Started** — FAQ, "don't have a bike" section (added My Kids Bikes + Cyclo note from raw site), 3 structured "other activities"
- **Help Out** — 5 role cards, "what joining looks like" honest commitment section (from raw volunteer page), routed contact form, start-a-chapter CTA
- **Chapters Overview** — map + list, Brussels clustering, Flanders hidden, start CTA
- **Chapter Page Template** — events auto-populated, team + volunteer form merged, optional sections hidden
- **About / Mission** — NEW: full 5-plane spec from scratch. Strategy, scope, structure, wireframes
- **About Overview** — deepened strategy, 6 sub-section cards, stat bar, CTA
- **About / Organisation** — organigram, 3 levels, coordination duo named, safety/routes, open questions resolved
- **About / Vision** — 4 policy demands, Child Friendly City coalition, parent quotes, open questions resolved
- **About / News** — article feed, article detail page wireframe, open questions
- **About / Press** — featured items, full list, auto-aggregation, dead link strategy
- **About / Partners** — 3 categories, 6 confirmed partners, "become a partner" CTA

## [2026-04-13] content | Activity Detail — Content template

Created `ux/activity-detail-content.md`: full copy template for the `/events/[slug]` page. Covers all 8 sections (hero, practical strip, what to expect, chapter context, team + volunteer ask, partners, photo permission, meta). EN/FR/NL for all static copy; database-driven fields marked as variables. Includes default "what to expect" body, two event-specific theme examples (Spooky Edition, Safety First campaign), and final photo permission copy.

## [2026-04-13] write | UX content pages — all 7 sections
All 7 UX wireframe pages now have companion content pages with full EN copy and FR/NL inline notes.

## [2026-04-13] restructure | UX wiki reorganisation

Restructured UX content into cleaner buckets. ux-planning.md now holds site-level content (principles, strategy, scope, sitemap, migration plan). Extracted all 14 page specs from the raw Notion dump into 7 files in ux/: activity-detail.md, events-overview.md, home.md, help-out.md, getting-started.md, chapters.md (overview + template), about.md (all about/* pages). Removed old plane files (_strategy, _scope, _structure, _skeleton) and the activity-detail subfolder.
