# Wiki Log

## [2026-06-02] build | Help out (P-13) — re-skin to the ride/show kit + real photos + copy joy

`/critique` of the first Help-out surface pass (Frederik, comparing the live Wix page): "the original gives a lot more happy vibes via text and photos." Diagnosis: my pass had (a) **no photography** (the original's hero photo + collage are most of the joy), (b) was **off the design system** — it used the warm-gradient + emoji-chip + contained-panel approach that was already rejected for Getting Started and that DESIGN.md marks as wireframe-placeholder, and (c) **cooler copy**. **Re-skinned to the ride/show kit** per DESIGN.md's new-page checklist: hero now reuses `.activity-hero*` (solid blue, daisy, **real volunteer photo** in the circle, "Doe mee" sky badge, -3° white headline); roles reuse `.activity-promises*` (yellow band, white tilted cards, **red Flux-icon chips** — shield-check/calendar-days/megaphone/camera/musical-note, emoji gone); added a **real-photo joy band** ("Dit is de bende", 4 photos, tilted) as the original's happy signal; the group picker is the climax on a light-blue band; quiet "start a group" coda. **Copy warmed up** (joyful, grounded, no emoji, 0 em-dashes): hero lead "Word deel van de ploeg die elke rit tot een feest maakt", warmer role + intro copy. **Photos** pulled from `docs/raw/assets/photography/` into `public/img/volunteers/` (6 files). All `.ho-*` CSS rewritten as thin deltas on the reused kit; old gradient/emoji block removed. **Verified:** `npm run build` clean, live render 200 (all bands + photos + red Flux chips render; debug bar confirms 0 exceptions; **0 em-dashes**), full suite **102 green**. **No screenshot** (playwright unavailable) — visual check is Frederik's; UI 🟠 ceiling. **Registry:** P-13 **Assets 🟠→🟢** (photos sourced; role illustrations moot — canonical Flux chips), UI stays 🟠; row + roll-up rewritten to the ride-aligned description. Wire/UI 🟢 still gated on Frederik's own critique. `[asset]` photo selection/cropping is a first pass — Frederik may want to curate.

## [2026-06-02] design | Steun flow — resolved the two open critique items (doc-only)

Closed the two items the `/critique` left for Frederik. **#3 nav label:** the support CTA is **"♥ Steun ons"** (heart icon) — the heart + verb-object disambiguate the *give* action from **Meehelpen** (volunteer); warm, not "Doneer". **#4 mobile (75% of traffic):** the button is **pinned as the first, accent-styled item at the top of the hamburger menu** (not buried, not a floating sticky that would compete with "vind een rit"). Applied to `20-structure.md` (nav model), `40-patterns.md` (PAT-10), `30-skeleton/steun-ons.md` (strategy note). Both critique items now resolved; the support UX plan is fully specced through Skeleton. Still doc-only — build backlog unchanged (terminology sweep + "♥ Steun ons" nav CTA + footer login move + `/steun-ons` page + contextual partial).

## [2026-06-02] design | DESIGN.md — documented the page-construction layout system

After the Getting Started surface pass needed a full reverse-engineer of the ride/show page to get the layout right, Frederik: "would it make sense to update DESIGN.md so future UI work is right quicker?" Yes. Added a **"Layout system (how a page is built)"** section + a **new-page checklist** to `DESIGN.md` (the file the impeccable skill loads first). It captures what was previously only implicit in `activities/show.blade.php`: the **full-bleed colour-band mechanic** (`100vw` breakout + nested `.container` re-align; first band `-8` top, last band `-8` bottom), **colour bands as a playful toolkit** (Frederik: deliberately **not** a fixed colour/section order — pages should vary; the activity sequence is one example, not a template; only soft guidance = adjacent bands distinct + page coherent), a **"reuse the section components, don't reinvent"** table (`.activity-hero*` / `.activity-promises*` / `.activity-info-map*` / `<x-event-card>`), the **canonical icon chip** (red rounded-square + white Flux icon; emoji = wireframe placeholder only), card spec (white, 1.5rem, alternating tilt), motion (IntersectionObserver stagger + reduced-motion opt-out), and the appearance-in-`app.css` rule. Also corrected the stale "card radius 0.75rem" line (content cards are 1.5rem; 0.75rem is the meta panel). Doc-only; no code or registry stage change.

**ROI decision (Frederik):** confirm + keep the volunteer charter public, but **don't link it on the public Help-out page** — only in volunteer onboarding / the logged-in back-office (D-1), and **host as PDF later**. Applied: removed the `vrijwilligersafspraken` Google-Doc link from the "wat we vragen" list (the bullet now just names the agreements: "Onderschrijf onze afspraken rond vriendelijkheid en veiligheid"); resolved help-out.md open-Q2; updated help-out-content.md. **Surface/UI pass (frontend-design skill):** applied the Kidical Mass design language (DESIGN.md) to the Help-out wireframe, mirroring the Getting Started precedent — **all appearance in `app.css` (`.ho-*`), Blade stays structural**, copy unchanged. Moves: full-bleed **warm gradient hero** (`person-with-boombox` illustration, -3° tilted headline, barrier-dissolver as the lead), **invitation role cards** with light-yellow -3° tilted emoji chips + hover lift, a full-bleed **light-yellow "wat meedoen inhoudt" panel** (two columns, green-check "krijgt" vs red-chevron "vragen" markers), the **group picker as the climax** (white pill grid, hover lifts to blue), and a deliberately **quieter "start a group" coda** (hairline-topped, muted, smaller heading — answers the critique's "secondary exit competes with the CTA"). Reuses the shared hero keyframes; hero elements added to the `prefers-reduced-motion` opt-out. **Verified:** `npm run build` clean (CSS bundle has the `.ho-*` rules), live render OK (all sections + illustration present, **0 em-dashes**, 0 Google-doc links), help-out test green. **No screenshot** (playwright unavailable here) — visual/aesthetic check is Frederik's; my render check tops out at **UI 🟠**. **Registry:** P-13 **UI 🔴→🟠** (Wire stays 🟠); row + roll-up (Surface-pass-done list) updated. Wire/UI 🟢 still gated on Frederik's own critique.

## [2026-06-02] design | Steun flow — /critique + fixes (doc-only)

Ran `/critique` on the just-documented support plan (design-plane, no UI yet), then applied Frederik's chosen fixes to `30-skeleton/steun-ons.md` + `design/10-scope.md`. **Fixed:** (1) **de-jargoned the ask** — the card now leads with the plain act **"Steun vanaf €3 per maand"** ("you're just supporting"); the t-shirt is the token beneath ("draag je steun"); **"Word Kidi Buddy" dropped from the headline** (too specialised — it stays the Growfunding tier name only). (2) **Dropped the backer count** ("Al 18+ mensen steunen mee") — reassurance now via **participation scale** ("honderden gezinnen elke maand"), pulled live, never a hardcoded count (small number undercuts + stale-stats trap). (3) **Riding-stays-free reassurance added to *every* contextual block** (Home + event-detail), not just the page hero — they reach more first-timers, so the "meefietsen blijft gratis" clause can't live only on `/steun-ons`; added a copy rule to enforce it. **Left open for Frederik** (named in the critique, not auto-applied): the "Steun" vs "Meehelpen" nav-label ambiguity (#3) and the mobile prominence treatment for the 75%-mobile audience (#4). Still doc-only; no site code touched.

## [2026-06-02] build | Getting Started (P-12) — surface/UI pass, aligned to the ride/show page

Frederik: "verbeter de UI van /nl/getting-started" then, after a first gradient-based pass, "get closer to the layout decisions I made in the event/ride show page — let me guide the decisions." So I did a **thorough review of `activities/show.blade.php` + its CSS + the live render**, surfaced where my first pass diverged (gradient hero vs his solid-blue daisy hero; contained white sections vs full-bleed colour bands; light-yellow emoji chips vs red Flux-icon chips; borderless rows vs white tilted cards; a reinvented alignment ladder vs his real `.container`), and put the open calls to him via **AskUserQuestion**. His four decisions: (1) hero = **match the ride hero**; (2) "wat je mag verwachten" = **mirror the promises band**; (3) **full-bleed colour bands**; (4) **keep the accordion**. **Rebuilt to honour them by reusing his actual ride-page classes, not reinventing:** the hero reuses `.activity-hero*` (solid-blue full-bleed, `logo-icon` daisy bleeding right, circular illustration on `--light-blue`, sky **WELKOM** badge, -3° white headline) — GS-only adds are `.gs-hero__badge/__lead/__circle`; the expectations section reuses **`.activity-promises*` verbatim** (sky-blue band, big tilted H2 + `kid-on-scooter` illustration, white tilted cards with red Flux-icon chips), 3 cards/column with a scoped nth-child(3) tilt + the same IntersectionObserver stagger; the FAQ stays the `<details>` accordion (contained white, red +/× marker); the CTA became a **full-bleed yellow band** with a -3° ink heading + blue button. Colour rhythm now reads blue → sky → white → yellow → blue partners → ink footer (the warm gradient + emoji chips + contained panels from the first pass are gone). Copy + IA unchanged; appearance stays in `app.css` per the public-site rule. **Verified:** `npm run build` clean, hero/expectations/CTA + mobile screenshots reviewed (faithful match to the ride band; cards reveal on scroll), tests green (PublicStructureTest 21/46), Pint clean, 0 em-dashes. **Registry:** P-12 **UI 🔴→🟠** (Wire stays 🟠), row note rewritten to the ride-aligned description. **Wire/UI 🟢 still gated on Frederik's critique.**

## [2026-06-02] design | Support flow ("Steun Kidical Mass") — terminology + prominence rework

Ran strategy + UX planning (Garrett planes, Socratic) on the **#1 org goal: bring money in**, at Frederik's prompt that the footer "Word lid" CTA is wrong (*"je wordt geen lid — iedereen kan deelnemen; je steunt Kidical"*) and the goal is **under-represented** (footer-only). **Doc-only pass — no code touched** (Frederik's choice); a `/critique` follows. **Decisions (via AskUserQuestion, plane by plane):** (1) **Strategy — terminology:** public verb = **"steun" / "Steun Kidical Mass"**, giver "steunt mee"; **"lid"/member retired sitewide**; "Spacefunding"/"Kidi Buddy" survive only as model/entry-tier names. Page `/membership` → **`/steun-ons`** (route name kept `membership`). (2) **Strategy — prominence:** elevated from the deliberate *quiet-footer-only* call to **a "Steun" nav CTA + contextual blocks + footer** — **supersedes** the org-goals "keep it quiet" trade-off, bought back with warmth + a **riding-stays-free reassurance on every touchpoint** (a loud ask must not read as "pay to ride"). (3) **Strategy — primary fan:** a **family that just rode** (warm, high-trust). (4) **Scope — one-off:** recurring stays the lead, but a **discreet one-off path is provisionally back in scope** (reverses the v1 cut) → new concern **`D-9`, pending Leticia** + IBAN (Notion, not public repo). (5) **Scope — t-shirt:** reframed as a **visible token of support ("draag je steun")**, not "your membership". (6) **Structure — nav:** the "Steun" accent button **replaces the login button** top-right; **login moves to the footer** (already there). 5 main-nav links unchanged. (7) **Structure — placement:** contextual "Steun" blocks on **Home + end of event-detail** only (getting-started + chapter pages dropped); all asks → `/steun-ons`. (8) **Skeleton:** full `/steun-ons` ASCII mockup + the reusable two-variant contextual block. Stopped at Skeleton (no Surface). **Files:** `strategy/{10-organisation-goals,01-concerns}.md` (goal #1 + trade-off reworked; S-9 amended), `design/10-scope.md` (§ Support rewritten), `design/01-concerns.md` (**D-9** added, Partly; gate still 0-Open), `design/20-structure.md` (nav model + sitemap + key-decision + content-model), `design/40-patterns.md` (PAT-9/PAT-10 reworked), **renamed** `30-skeleton/membership.md` → **`steun-ons.md`** (rewritten), `30-skeleton/00-page-registry.md` (P-04 → "Steun Kidical Mass" `/steun-ons`; global-shell roll-up flags the planned-but-unbuilt rework), `glossary.md` ("steun" public, "lid" → don't-use). **Code untouched** (footer still renders "Word lid" → `/membership`; nav still shows login) — build work = terminology sweep + nav CTA + footer login move + the page + the contextual partial.

## [2026-06-02] build | J2 confident seam — Help-out picker + chapter sign-up form + critique fixes

Built the full J2 conversion seam end to end (Frederik: "build that, fix the wireframes, write proper copy"), after a `/critique` of the Help-out wireframe. **Critique fixes (Help out):** purged all em-dashes from rendered copy (added `assertDontSee('—')` guard), renamed "afdeling" → **"lokale groep"** (the duo's NL term), **resequenced** so the inspiring roles (not the obligations) are the last beat before the action, gathered the roles back at the CTA, restored the "honderden" scale signal. **The seam:** the CTA is no longer a button to the family `/chapters` index — it's an **inline group picker** (tap your group → `/chapters/[id]?intent=volunteer#aanmelden`); no map detour, no typing. Help out is now controller-backed (`VolunteerController` lists visible groups); route changed `Route::view` → `Route::get`. **Chapter form built:** replaced the `<x-wire.placeholder>` with a new **`ChapterVolunteerSignup`** Livewire component (name/email/**role checkboxes** incl. "Nog niet zeker"/message → `ContactForm`; warm "Je bent erbij" + next-ride `<x-event-card>` confirmation; `#aanmelden` anchor; `?intent=volunteer` welcome banner that leads with the form). **Deliberately a separate component** from the event-detail `VolunteerSignup` (shared with J1) — that one is untouched. `[backend remainder]` the enquiry emails the **central comms inbox tagged with the chapter name + roles**; true per-lead routing waits on a per-group lead email on the `Group` model (no schema change made). **Verified:** full suite **102 green** (+3 new `ChapterVolunteerSignupTest`: submit-tags-group-and-roles, validation, honeypot; +Help-out section/em-dash asserts), Pint clean, live render confirmed (picker = 8 group links w/ intent; chapter page shows form + welcome + 0 em-dashes; welcome absent without intent; event-detail still 200). **Fixed 2 pre-existing `BuildStatusTest` failures** (`pagesTotal`/count hardcoded 21 → 22) left stale by the concurrent Find-a-bike P-22 addition — not caused by this work. Registry P-11 + P-13 top-gaps updated; skeleton docs (`help-out.md`, `chapters.md`) note the build + the per-lead-routing remainder. **Still open:** per-lead routing (client/Nico), the chapter page's own **full NL pass** (it's still EN around the new NL form), is-the-ROI-public, UI/Surface pass + Frederik's Wire 🟢 critique.

## [2026-06-02] build | Footer + partners NL-localised, fat-footer rebuilt to IA, privacy/cookies folded

Frederik: the footer + partners block were still hardcoded English while the rest of the site is NL — fix that; then review the fat-footer **structure** against the new IA/scope/journeys; wire the social links (Instagram + Facebook only); fold the two legal pages into one. **Localisation:** moved all copy into NL lang files (new `lang/nl/footer.php`, `lang/nl/partners.php`; reused `nav.*`/`common.*`), wired `footer.blade.php` + `components/partners.blade.php` to `__()`. **Fat-footer rebuilt to mirror `20-structure.md`** (was a grab-bag: Home + Contact + Membership + Privacy + Cookies + Login in one column, plus three dead `href="#"` org links): now *Ontdek* = the 5-item main nav, *Over ons* = the About sub-nav, brand column carries the **persistent "Word lid" CTA → /membership** (IA: membership is a persistent footer CTA + top org objective), and a utilities bottom bar (Contact · Privacy & cookies · Inloggen). Dropped off-strategy **"Donate"** (one-off donation is dropped for v1 — support is recurring membership) and the duplicate `mailto:` Contact. **Social:** reduced to Instagram + Facebook (LinkedIn removed), wired to the live accounts (IG `kidicalmass.belgium`, FB `Kidicalmass.brussels`, confirmed by Frederik). New `.site-footer__cta` (yellow pill) in `app.css`. **Legal fold:** privacy + cookies are now **one page** — `/privacy` titled "Privacy & cookies" with both sections; `/cookies` 301s to it (preserves any links indexed from the old Wix site); `cookies.blade.php` removed. **Verified:** `npm run build` clean, footer screenshot reviewed (all NL, 2 socials, single legal link), tests green (NavigationTest updated: footer asserts one legal link + a /cookies→/privacy 301 test; PublicStructureTest drops `/nl/cookies` from the 200 sweep), Pint clean. **Docs:** `20-structure.md` (footer cluster + sitemap + key-decision lines reflect the fold), `00-page-registry.md` (P-06 gap note = folded + still stub; roll-up notes the legal fold + a "global shell" line for the footer/partners work — the footer is shell chrome, not a P-row, so no page-stage emoji flips). **Still open:** legal text itself is still a stub (P-06 unchanged 🔴/❓); FR mirrors (`lang/fr/footer.php`, `lang/fr/partners.php`) needed when the FR layer lands; the yellow CTA styling is Frederik's to critique.

## [2026-06-02] build | Em-dash purge + tone-of-voice rule; police wording confirmed

Frederik: the copy had too many em-dashes, "a clear tell of AI-generated content" — fix the copy and add the rule to the tone-of-voice doc. **Copy:** de-dashed all rendered NL copy on Getting Started (6) and Find a bike (5), rewriting each into natural Dutch (full stop for two thoughts, comma for an aside, colon to introduce) rather than a mechanical swap. Verified 0 em-dashes in the live HTML of both pages. **Tone of voice:** added an em-dash rule to `docs/tone-of-voice.md` "What to Avoid" (banned as a connector in all interface copy, every language; compound hyphens fine), and de-dashed the doc's own prose + examples to practice it — the four voice-quality headings now use the parenthetical form already used in CLAUDE.md ("Joyful (not frivolous)"). Only the rule line keeps an em-dash, referentially. **Police wording CONFIRMED** (Frederik): resolved P-12 open-Q #9 and the content-companion safety-FAQ note from 🚩 to ✅ — "waar nodig stemmen de organisatoren de route af met de lokale politie" stands. **find-a-bike slug** confirmed (no change). No registry stage changes (copy refinement, not a stage bump). Tests green (22), Pint clean. Blade `{{-- comments --}}` still contain em-dashes (dev notes, not rendered) — left as-is.

## [2026-06-02] build | Help out (P-13) — NL orientation wireframe (J2 reframe)

Built the **Help out** page (`resources/views/volunteer.blade.php`, NL, wireframe fidelity) from the just-reframed [`help-out.md`](design/30-skeleton/help-out.md), at Frederik's request to "move further + get NL wireframes." Was a 2-line stub ("Meer info volgt binnenkort"). Now the **J2 orientation page**: header → pitch (barrier-dissolver "je hoeft geen fietsexpert te zijn") → 5 role cards as invitations (roze hesje · mede-organisator · communicator · fotograaf · DJ, emoji icons like Getting Started) → "Wat meedoen inhoudt" honest get/ask (distilled from the ROI principles; "vrijwilligersafspraken" links the guidelines doc at the *commitment* moment) → **single primary CTA "Vind je afdeling" → /chapters** → calm "nog geen afdeling?" secondary exit (mailto + chapters link). **No contact form** — the reframe routes via the chapter; the form lives on P-11. Mirrors the Getting Started conventions exactly (`<x-layouts::site>`, raw headings, semantic HTML, structural classes only, Surface deferred). **Verified:** live render at `/nl/help-out` (curl confirms all sections; old stub gone), `PublicStructureTest` +1 test (22 passing, asserts the sections + that the page routes to `groups.index`), Pint clean. **Registry:** P-13 **Wire 🔴→🟠**, **Back ❓→⚪** (page is now static — the routed form + backend live on P-11), top-gaps + roll-up updated. **No screenshot** (playwright unavailable in this env) — visual/aesthetic check deferred to the Surface pass + Frederik critique (UI 🔴). **Chapter-page form (P-11) deliberately not touched** — it's an honest `<x-wire.placeholder>` on an already-rebuilt, model-backed, shared-tree page; turning it into the real role-checkbox form is a Back task (needs the `Volunteer enquiry` model + routing), proposed as the next step rather than faking a non-functional form.

## [2026-06-02] build | Find a bike (P-22) — no-bike detail split off Getting Started

Frederik: the "Geen fiets?" block "feels odd" sitting on Getting Started even after the distill — *"fold a short version into the FAQ and give it its own page."* Agreed (a rental-shop list is reference material, not conversion material). Scope chosen (AskUserQuestion): **no-bike providers only** — the earlier-relocated "Andere manieren" stays parked for a later/different home, not merged here. Built **new page `find-a-bike.blade.php`** (`/find-a-bike`, route name `find-a-bike`, NL, wireframe fidelity): Kidical Mouse, Loopz, Fietsbieb, My Kids Bikes + Cyclo note + a "een fiets gevonden? → /events" CTA and a back-link. **Standalone, deliberately *not* in the locked 5-item nav** — reached from the Getting Started FAQ, reusable later from event/chapter pages. On Getting Started the section is **gone**; a single FAQ item ("Wat als we geen fiets hebben?") keeps the brand promise on-page and links out. Registry: **new row P-22** (UX 🟢, Conf 4, Wire 🟠, UI 🔴), P-12 top-gaps updated, roll-up 21→22 rows / 16→17 UX 🟢. Tests: added `/nl/find-a-bike` to the 200 sweep + a content test, and split the moved assertions (KIDICALMASS/€30 now asserted on find-a-bike, `assertDontSee('KIDICALMASS')` + the FAQ-link assertion on Getting Started) — both-way leak checks. All green, Pint clean, dashboard parses 22 rows with no warnings/drift. Slug `find-a-bike` is easy to rename. `[client]` Kidical Mouse availability still open.

## [2026-06-02] design | J2 "become a volunteer" flow — folded into design/ + ux-folder migrated

UX pass on **journey J2** (potential volunteer → routed local contact) at Frederik's request, picked as a flow that doesn't collide with the parallel J1 "newcomer → first ride" thread (J2 owns Help out + the chapter-page form; J1 owns Home/Events/Event detail). Worked the planes top-down via AskUserQuestion. **Core reframe (Frederik's choices):** routing is **wayfinding through the chapter**, not a form field — so **Help out becomes a pure orientation page** and the **contact form lives on the chapter page** (routed by context). This *reconciles* the stale `help-out.md` (which still put a form + municipality dropdown on Help out) with the already-decided `20-structure.md` ("the volunteer contact form lives on chapter pages") and `chapters.md`. **Decisions:** (1) confirmation promises **"the team", no name/SLA** — deliberately independent of the unvalidated D-1 reply-loop assumption; (2) the **post-submit limbo** is answered with the chapter's **next-ride hook** (reuses ride data already above the form); (3) **role checkboxes** (incl. "Not sure yet") carry onto the chapter-page form (matches the `Volunteer enquiry` entity); (4) "start a chapter" stays a calm **secondary exit** (anti-overlap, avoids J3 territory). **ROI/guidelines repurposing:** the charter is organiser/escort (P4/P5) operational material — only the **🧭 principles** are repurposed (kindness, inclusion, safety) + **"Action positive" becomes a tone constraint for the whole flow** ("positive, not preachy"); the full doc is a **commitment-stage external link** renamed "volunteer guidelines", not an entry gate; operational detail → onboarding/back-office (D-1). **Files:** rewrote `help-out.md` (orientation reframe, J2 flow diagram, guidelines table, open Qs), updated `chapters.md` (form role checkboxes + resolved confirmation state — closes its open-Q #4), `help-out-content.md` (form copy relocated, guidelines moved to commitment, role copy kept), `20-structure.md` (Help out sitemap node). No new `01-concerns` entry — decisions are made; client/build remainders live in the page Open-Questions sections (preserves the 0-Open gate). **ROI captured** to `docs/raw/website/volunteer-roi-charter.md` (FR + NL verbatim) with the **named-signatory list redacted** per the public-repo guardrail — ⚠️ note the [2026-06-02 P-12 entry below] recorded "ROI not committed to the public repo"; this save is **working-tree only, signatories redacted**, pending Frederik's call to keep or pull. **Migrated + removed the orphan `docs/ux/` folder** — its content was already in the Cascade wiki and in places stale (e.g. "NL/FR/EN all in scope" vs decided **D6** NL+FR): `activity-detail/` was already fully in `30-skeleton/activity-detail.md` (newer, with the PAT-18 note); root `_strategy.md`/`_scope.md` superseded by `strategy/20-personas`, `strategy/10-organisation-goals`, `strategy/00-strategy-brief` (D1/D3/D6), `design/10-scope`. No registry stage bumps (this is re-planning, not a build).

## [2026-06-02] build | Getting Started (P-12) — safety reassurance from the volunteer ROI + Jorge interview

Frederik reviewed the volunteer **Règlement d'Ordre Intérieur** (organiser/accompagnateur charter) for first-timer-relevant content. Verdict: it's J2/J3 material (volunteer/organiser), **not** J1 — ~90% belongs to Help-out (P-13) / back-office (P-09) / Mission values (P-15), and dumping it here would undo the distill. But **safety is the first-timer's top anxiety**, so two concrete, public-safe facts were surfaced surgically (no new sections beyond one FAQ): (1) the "trained volunteers" fact card became **"Minstens vier roze hesjes"** — front/back + every junction clear (ROI: min. 4 marshals, ~1 per 10 riders, capitaine-ouvreur + vélo-balais); (2) a new FAQ **"Is het veilig in het verkeer?"** carries the **police-coordination** reassurance Frederik recalled from the **Jorge interview** — phrased "waar nodig stemmen de organisatoren de route af met de lokale politie" (matches ROI's *"coordonner le parcours avec les autorités locales si besoin"*). Pace number (8–9 km/h) deliberately **not** added — qualitative "kindertempo" reassures better than a number a non-cyclist will second-guess. **Guardrail:** the ROI carries named signatories + governance → **not committed to the public repo, not saved to Notion** (per Frederik); only the generic safety facts surface publicly. New open question logged (P-12 #9): confirm public police wording with Frederik/Leticia — involvement varies by commune. Updated `getting-started.blade.php`, content companion (card 6 + new FAQ, EN/FR/NL), skeleton open questions, test (`assertSee('lokale politie')`). Tests green, Pint clean. **Still to route:** ROI is the source charter for the J2/J3 volunteer/organiser builds — pointer not yet dropped in those briefs.

## [2026-06-02] build | Getting Started (P-12) — distilled (Frederik critique)

`/critique` on the built page (Frederik): *"a ton of content sits between the intention and the CTA; 'Andere manieren' doesn't belong here, and 'Geen fiets?' neither."* Diagnosis: the page was doing three jobs (reassure a first-timer / solve a no-bike barrier / be a cycling-resource hub) and burying its one CTA under the latter two. Applied **`/distill`**: (1) **CTA "Vind je rit" lifted** to directly after the FAQ — the natural "yes" moment; (2) **"Andere manieren om te fietsen" removed** from the page (it sent first-timers *away* at the decision point — relocated to the content file for a future resources/chapter spot); (3) **"Geen fiets?" demoted below the CTA** and slimmed from a 4-card grid to a plain list (brand promise kept, off the critical path). Live order now verified: CTA precedes the no-bike block; "Andere manieren" absent. New flow = reassure → answer → **go**, edge case as a footnote. Updated `getting-started.blade.php`, the skeleton Structure + Scope notes, the content file (relocation note), test (added `assertDontSee('Andere manieren')`), and P-12 top-gaps. Tests green, Pint clean. Wire/UI 🟢 still pending the Surface pass. Visual/AI-slop audit deferred — no surface yet.

## [2026-06-02] build | Getting Started (P-12) — first static page off the stub list

Ran the **newcomer → first ride** flow (J1 entry, flow A) at Frederik's request ("UX planning + wireframing for one user-story flow"). Audit found the flow's planning already complete — `getting-started.md` is ✅ with full desktop+mobile skeletons + trilingual content — but the **view was a bare `<x-stub>`**, and the Home hero's "New here? Start here →" pointed at `#upcoming`, **bypassing the page entirely**. Two fixes shipped: repointed the Home entry link to `route('getting-started')`, and **built the NL wireframe** (`resources/views/getting-started.blade.php`) from the skeleton — header, 6 what-to-expect fact cards, 6-item FAQ, "Geen fiets?" (4 providers), "Other ways to cycle" (3), CTA → /events. Wireframe fidelity (semantic HTML, structural layout only; Surface/UI pass deferred). **Re-plan caught stale facts** (re-verified, sources in new `getting-started-research.md`): **Fietsbieb** changed 1 Jan 2026 → **€30/yr (€10 reduced) + €20 deposit, now Vlaanderen+Brussel** (was "€20/yr, Brussels-only"); **Loopz** domain loopz.be→**loopz.bike**; the content file's **"helmet required under 12" note was factually wrong** (no such Belgian law) and removed; My Kids Bikes + Pro Velo Families-on-Bike confirmed active (locations softened). **Open:** Kidical Mouse ride-availability (web-unverifiable → client). **Scope flag:** the "don't have a bike" provider block was deferred out of v1 core on 2026-06-02 pending verification — verification now done, so it's built and ready; ship-in-v1 is Frederik's call. Tests: repointed the stub-assertion to `/nl/about/mission`, added Getting Started section coverage + the Home entry-link assertion (PublicStructureTest, all green). **P-12** Wire 🔴→🟠, Conf 3→4, Assets 🔴→⚪ in `00-page-registry` (+ roll-up); dashboard parses clean. Wire/UI 🟢 still gated on Frederik's critique + the UI pass.

## [2026-06-02] design | Content cross-check — old Wix bodies vs new structure

Cross-checked the new structure against the raw Wix scrape (`docs/raw/website/`) at Frederik's request ("make sure we didn't lose anything; are there content gaps?"). **Page-level: complete** — every old page already has a destination in [`25-content-migration`](design/25-content-migration.md); nothing whole is lost. Reading page *bodies* surfaced content-fragment risks the title-map hides, now logged in `25-content-migration` § Content-level cross-check. **Biggest risk:** chapter pages launch near-empty — only **Namur (/5000)** + **Mons (/7000)** have real scraped content; the ~13 Brussels postal pages aren't in the scrape (audit confirms "not one has its own page"), so cutover needs *content authoring* by leads, not a data move. **Structural fixes applied** to `20-structure`: chapter template gained an **intro / "what our rides are like"** block + an optional **History / our story** block (both used by the real Mons/Namur pages, both missing from the template). **Three decisions (Frederik):** (1) **Liège + Mons both hosted as full chapters** — *reverses the locked "Liège = external pin" call* (updated `20-structure` key decisions + chapters node, `25-content-migration` cross-check + Decision Flag #5); (2) **one-off bank donation (BE72…) dropped for v1** (recurring-only stands; confirm with Leticia); (3) **"don't have a bike" provider section deferred** out of v1 core (provider list — Cyclo/Fietsbieb/Loopz/My Kids Bikes vs our "Kidical Mouse" — to verify later). Other fragments flagged to carry: the canonical "what is a ride" spec (no draisiennes, accompanied child, 5–7 km, free) → Getting Started + event defaults; Vision needs rewrite + fresh NL (FR-only, dated 2024-elections, Brussels coalition); volunteer ROI doc + Safety-First video → back-office (D-1); national funders + #StreetsForKids affiliation → Partners; contact `.be`/`.brussels` domain split to resolve at build.

## [2026-06-02] design | Structure sweep — sitemap validated + logged-in IA locked

Full Structure-plane (plane 3) review at Frederik's request ("get the sitemap right; public + logged-in"). **Coverage check:** traced every [persona](strategy/20-personas.md)/[JTBD](strategy/30-jobs-to-be-done.md)/[journey](strategy/50-user-journeys.md) to the canonical sitemap — public IA holds, no scope creep. **Three gaps caught + homed (Frederik):** (1) **per-region email subscription** (a Scope feature) had no home → control on Events + chapter pages; (2) **legal/utility pages missing** → `/privacy` + `/cookies` (GDPR — enquiry form + email opt-in) + 404 in a footer cluster; (3) **no national contact front door** → new `/contact` → coordination duo (press/partnership/general buckets), distinct from chapter-routed Help out. **Logged-in IA locked (3 forks):** (a) **back-office = separate branded frontend surface** (`/backstage/[postal-code]`, read-mostly), **not** Filament — volunteers (P4) never touch `/admin`; (b) **accounts invite-only** — leads provision in `/admin`, **no public Register**, login link only, post-login landing = My activities; (c) recorded as a **three-tier** model (public / logged-in frontend / admin CMS). Cascaded across `design/20-structure` (nav + footer, full three-tier sitemap, 6 new key decisions, 2 new content-model entities — Contact message + Email subscription), `design/30-skeleton/00-page-registry` (5 new route rows), `design/01-concerns` (D-1 boundary decided; content detail still pending Alexandre). **Deleted** the stale `docs/ux/_structure.md` (a pre-Cascade duplicate that still said "Contribute"/News-as-nav/3 languages/no logged-in pages). Remainder: back-office *content* brief still waits on the Alexandre/J3 interview (D-1).

## [2026-06-02] design | D-4 — surface tokens closed (DESIGN.md documents app.css)

Item 4 of the design review walk. **Finding:** the D-4 premise ("no machine-readable tokens; brand in prose only") was outdated — a full Tailwind v4 `@theme` token set is **live in `resources/css/app.css`**, set in the merged `design/activity-page` redesign (verified: branch is fully contained in main, app.css identical). **Decisions (Frederik):** (1) **`app.css` is canonical** (where Tailwind reads tokens); a new **`DESIGN.md`** at repo root documents it (no duplicated hex → no drift); (2) the as-built palette + type is the **approved** brand system (follow the redesign branch). Wrote **`DESIGN.md`** — palette (ink `#281a39`, yellow `#f9d924`, blue `#1d67cd`, + red/green/orange/sky/light-blue/light-yellow, zinc neutrals), semantic tokens, typography (**Poppins** headings / **Nunito Sans** body / **Fredoka One** logo), and the signature design language (−3° tilts, red rounded-square icon chips, full-bleed colour blocks, yellow→orange gradients, animated yellow link underline, oklab color-mix tints). **De-staled `50-surface.md`** (was "dark blue+white+yellow" + TBD sketch → corrected palette/type, points to DESIGN.md as built). `D-4` **Open → Closed**. Updated `40-patterns` gap note, `00-design-plan` Surface row (🟡→✅), `01-concerns` (now **0 Open / 3 Partly / 2 Closed** — no fully-Open design concern remains), and `index.md`.

## [2026-06-02] design | D-7 — redirect map drafted (Open → Partly)

Item 3 of the design review walk (D-1 parked pending Alexandre). Created **`design/26-redirect-map.md`** — every old Wix URL → new route, all `301`, derived from the migration plan + site-audit inventory. **Language rule decided (Frederik):** redirects target **neutral paths**; a **locale middleware** resolves `/nl/`|`/fr/` via Accept-Language → cookie → geo, **fallback NL** — so the map stays language-neutral. Proposed Drop-targets confirmed: gallery → `/`, downloads → `/about`, shop (`/product-page/*`, `/category/all-products`) → `/membership`. Unmapped legacy paths → 404 (no masking). `D-7` moved **Open → Partly** — design side resolved; remainder is build-only (middleware + 301 config, fill Grande KM event `{slug}`s at seed, confirm the two combined-postal canonicals, preserve `/post/{slug}` slugs, post-launch crawl of the critical set `/agenda` + postal pages). Design register now **1 Open (`D-4`) / 3 Partly (`D-1`,`D-3`,`D-7`) / 1 Closed (`D-2`)**. Updated `25-content-migration` (redirect open-question → resolved pointer) and `index.md`.

## [2026-06-02] design | D-2 review — closed (meetups public, chapter pages only)

Design-phase review pass, item 1 of the walk (D-2 → D-1 → D-7 → D-4 → verification → bookkeeping). Settled the three open sub-questions on meetup visibility (Frederik): **(1) status — treat as locked** (no further client gate, accepted as diverging from Leticia's "strong local communities" stance); **(2) detail level — fully public, all details** incl. meeting point; **(3) where surfaced — chapter pages only** (no national movement/aggregation view, not on the `/events` ride calendar; a logged-in volunteer's cross-group view stays in My activities). `D-2` moved **Partly → Closed** (design register now 2 Open / 2 Partly / 1 Closed). **Fixed a stale drift:** `20-structure` sitemap still showed "logged-in account also sees meeting/workshop activities" on `/events` (pre-revision model) — removed; `/events` is rides-only. Cascaded across `design/01-concerns` (D-2 → Closed + at-a-glance/gate), `design/10-scope` (MVP core, viewing rule), `design/20-structure` (sitemap + key decisions), `my-activities` brief, and the `strategy/01-concerns` S-4→D-2 lineage. `strategy/00-strategy-brief` D8 already encoded public viewing — unchanged.

## [2026-06-02] strategy | Locked 3 Leticia-grade signals + movement-facts block

From the signal-vs-strategy walkthrough, locked (Frederik): **(1) "the site mirrors the org's governance"** (local → national) — added as a design principle; **(2) attendance display = hosts/organisers only, not the full attendee list** (Leticia) — applied to D-1, scope, PAT-18, and the Attendance content-model row; **(3) funding rationale "subsidies won't always flow"** — added to org-goal #1. Added a compact **Movement facts** block to `10-organisation-goals` (~50 events/season, ~300 active, ~15–20 groups [13 on Growfunding], ~500 mailing list, ~75% mobile). **Declined:** "Local > mass" (not locked). **Held as signal for Alexandre:** volunteer role ambiguity + post-onboarding pain (feed D-1).

## [2026-06-02] decision | Meetups are public (revises D-8/D-9)

Frederik decided meetups/workshops are **publicly viewable** (not logged-in-only) as a traction/recruitment signal — resolving the open `D-2` sub-question. This **revises `D-8`/`D-9`:** login no longer gates *viewing*; it gates **attendance + the back-office** only. Cascaded across `strategy/00-strategy-brief` (D-8/D-9), `design/01-concerns` (D-2 → decided: public, cross-group), `design/10-scope` (viewing rule, accounts, build implication), `design/20-structure` (content model, group_user, sitemap, key decisions — dropped the meetup view-gate field), `glossary` (meetup + group_user rows), and the `my-activities` brief. Remaining design detail: *where* public meetups surface (chapter pages + a movement view, kept out of the family ride calendar) and the default-municipality filter (to test).

## [2026-06-02] strategy | Review pass — fact-checked vs primary sources, tightened, D-1/D-2 decided

Reviewed all 7 strategy pages against the primary sources (Leticia meeting + the two completed volunteer interviews, Jorge/Woluwe & Morgane/Auderghem). **Guardrail applied (per user): interviews are signal, not strategy** — reframed interview-derived assertions as unvalidated signal in `20-personas` (P4) and `50-user-journeys` (J2); added a note to `01-concerns`. **Fixes:** corrected the chapter count (`~45` → `~15–20`; 13 on the Growfunding page); "Five"→"Six" actors; **compressed `30-jobs-to-be-done` to a table** (was the heaviest/least-unique doc); removed the "no bike? no problem" subhead detail from `40-value-proposition`; source-hygiene across files (dead `wiki/service-design`, `wiki/strategy-plan`, `wiki/ux-planning` refs → `notion`/new locations); updated J3 evidence note (both interviews are pink-vests → J3/chapter-lead still untested, Alexandre pending) and fixed the `S-3`→`D-1` pointer. **Strategic calls recorded in the Design register (canonical):** `D-2` **resolved cross-group** — any logged-in volunteer sees all groups' meetups (traction + recruitment rationale; default-filter to own municipality, to test; open sub-question: should meetup existence be *public* for non-logged-in visitors — would revisit `D-8`). `D-1` **validated as a design input** — a two-layer per-chapter volunteer back-office (what-to-expect pre-signup; how-it-works/docs/video/meetup-schedule/chapter-leads-and-roles post-login), detail pending the Alexandre interview (may request his material). Updated `design/01-concerns` (D-1/D-2 Open→Partly), `design/10-scope`, and the `my-activities` brief.

## [2026-06-02] tidy | Convention cleanup (phase frontmatter, voice dial, key-decisions fold)

Cleared the outstanding tidy items. **Phase frontmatter:** added `phase:` to the 19 legacy files that lacked it (site-audit → discovery; tone-of-voice → cross-cutting; all `design/30-skeleton/*` + the two asset maps → design; key-decisions → strategy) via a deterministic script. **Voice gate:** added a **Register dial** (joyful/warm ←→ serious/factual, hard stops at corporate + militant) and a **Pre-ship checklist** to `tone-of-voice.md`, so the glossary's forward-reference to "register dial + pre-ship checklist" now resolves; fixed the "espacefunding" → "spacefunding" typo. **key-decisions fold:** `git mv key-decisions.md → strategy/90-key-decisions-evidence.md` (90 = evidence per Cascade numbering), reframed its header to mark the [Strategy Brief](strategy/00-strategy-brief.md) canonical and itself as underlying evidence; updated the `index.md` row and the two strategy frontmatter `sources` pointers. Link check clean.

## [2026-06-02] gaps | Resolved 3 gap clusters via interview (mission/VP, back-office/visibility, missing briefs)

Worked the prioritised gap list with the user. **Mission/VP (S-1/S-2):** locked working versions (Leticia sign-off still pending, so kept Partly) — hero leads on joy/scale ("Join thousands of families taking over the streets…") + inclusivity subhead; internal mission is the 4-job statement (find · participate · run · **fund**, the funding clause added per user). **Back-office/visibility (recorded as provisional leans, pending Alexandre/J3 + Leticia):** `D-1` → minimal per-chapter back-office **+ attendance**, provisionally IN scope (was a not-settled cut); `D-2` → cross-group meetup visibility. **New cross-cutting rule:** "I'm coming" attendance is **account-only and volunteers-only**, on **all** activity types (rides + meetups) — a volunteer roster, not a family headcount — adding an **Attendance** entity (volunteer↔activity). **Accounts clarified: volunteers only** (families have none; spacefunding members pay externally, no site account). **Two missing briefs written:** `30-skeleton/membership.md` and `30-skeleton/my-activities.md`. **Growfunding reality check:** fetched the live campaign — platform is **Growfunding** (not "Growthfunding"), product is **Spacefunding**, recurring only (monthly/annual), 6 tiers €3–€500/mo, entry €3 "Kidi Buddy" = t-shirt, €20+ tiers grant logo placement; membership page **links out**, no on-site payment. Updated `glossary.md`, `strategy/{01-concerns,40-value-proposition}.md`, `design/{01-concerns,10-scope,20-structure,40-patterns}.md`, the page registry, and reconciled `activity-detail.md` (volunteer attendance moved from "deferred" to provisionally IN; family RSVP still out).

## [2026-06-02] glossary | Settled the public word for a ride (parade vs rit)

Resolved the long-standing NL wording question. Swept the full Wix scrape and the new-site drafts for every candidate term. Finding: the coordinators' own published copy is consistent — **parade / fietsparade** *defines* the thing on every mission/about/stats page (`le-projet-het-project.md`: "organiseert feestelijke **fietsparades**"; homepage: "feestelijke, veilige **fietsparades** op kindermaat"), with **balade / fietstocht** as the warm in-motion word and FR **cortège** for the escorted procession. **`stoet` appears zero times anywhere** (and reads as carnaval/begrafenis) — the old glossary value "stoet / rit" was ungrounded. `rit` was only incidental in coordinator copy (1×, a DJ blurb); it had crept into the new-site CTA drafts as Frederik's choice. **Decision (Frederik):** NL public = **fietsparade** (concept) · **fietstocht / tocht** (in motion); drop **stoet, rit, optocht**. FR = **parade cycliste festive** · **balade** · **cortège**. EN internal "ride" unchanged. Updated `glossary.md`; sweeping `rit`/`optocht` → `fietstocht`/`fietsparade` across the NL `design/30-skeleton/*-content.md` + `*.md` drafts.

## [2026-06-01] restructure | Cascade Design phase re-shelved (planes)

Re-shelved the Design phase onto Garrett's planes. **Moved (git mv, history preserved):** `ux/` → `design/30-skeleton/` (7 specs + 7 content companions); `image-map.md` → `design/60-asset-map.md`; `image-map-new-site.md` → `design/61-asset-slots.md`. **Split** `ux-planning.md` (deterministic script) into `design/10-scope.md` (plane 2), `design/20-structure.md` (plane 3 — nav, sitemap, **+ new content-model table**), `design/25-content-migration.md`; its Strategy section was dropped (superseded by the Strategy phase) and its Principles folded into `design/00-design-plan.md`; `ux-planning.md` removed. **New artifacts:** `design/40-patterns.md` (PAT-1…PAT-17 with stable IDs), `design/50-surface.md` (visual direction; flags that `DESIGN.md` tokens don't exist yet), `design/30-skeleton/00-page-registry.md` (per-route spec/content/build/lifecycle tracker — build status marked unverified pending a pass with Nico), `design/01-concerns.md`. **Lint/reconciliation:** strategy concerns `S-3`/`S-4`/`S-5` **graduated** to design `D-1`/`D-2`/`D-3` (canonical there) now that Design is open; strategy register updated (0 Open, 2 Partly, 5 Closed, 3 Graduated). Fixed all internal links (depth changes in moved skeleton files, renamed asset-map cross-refs, strategy→design pointers) and rebuilt the `index.md` Design section. Surface tokens (`D-4`) and the Membership / My-activities briefs remain the notable design gaps.

## [2026-06-01] restructure | Adopt Cascade structure — Phase 0 scaffold + Strategy phase

Began migrating the wiki onto the **Cascade** playbook (phases Discovery → Strategy → Design → Build; planes within Design). **Phase 0 (scaffold):** created `discovery/`, `strategy/`, `design/`, `build/`, `_archive/`; added `glossary.md` (NL/FR vocabulary + don't-use terms, flags the spacefunding/Growthfunding ambiguity); added phase plan/status stubs to discovery (pointer — discovery stays in Notion, out of git per user), design (maps where each plane currently lives), and build (YAGNI, not opened); documented the Cascade schema + page conventions in `CLAUDE.md`. **Strategy phase (built out):** `00-strategy-brief.md` (locked decisions D1–D9 distilled from key-decisions + ux-planning + the Leticia interview), `01-concerns.md` (keystone register — 2 Open / 3 Partly / 5 Closed, stable `S-n` IDs), `10-organisation-goals.md` (ranked, money-in #1), `20-personas.md` (5 actors + anti-overlap contract), `30-jobs-to-be-done.md` (new), `40-value-proposition.md` (new — overall + per-persona, S-1/S-2 drafts), `50-user-journeys.md` (J1–J3, J3 evidence gap flagged). Rebuilt `index.md` with a Phase column. **Deliberately skipped:** Discovery re-shelving (kept in Notion) and Design/Build content re-shelving (a later pass). Sources read: key-decisions.md, ux-planning.md, and the deleted-but-in-HEAD service-design.md + strategy-plan.md (recovered read-only, not re-committed).

## [2026-05-18] ux-planning | Internal meetups / activity types folded into scope

Gap found: the interview revealed Leticia's internal meetups + that Nico already built an `Activity` model (types `kidicalmass`/`meeting`/`workshop`/`other`, bilingual NL/FR, group-linked) with a separate `Article` model and a `group_user` pivot — none of which was in ux-planning (Events only ever covered public rides). Verified on `origin/main`; no visibility field exists yet. Applied 3 user-confirmed decisions: (1) non-ride activities visible to any logged-in account across all groups; (2) one Events system gated by type (public rides + account-visible meetups, chapter page + a "My activities" account view); (3) keep "group-volunteer account" distinct from "spacefunding paying member." Updated Scope (Core, new Activities + Accounts functional blocks, Content Requirements), Structure (sitemap + Key Structural Decisions), and added a build implication for Nico (needs a visibility rule) + a "validate with Leticia" open question (cross-group visibility is broader than her local-communities stance).

## [2026-05-18] ux-planning | Strategy & scope updated from Leticia interview

Applied four user-confirmed decisions + several confirmations from the interview. Strategy: added "bring money in" as the top organisational objective (recurring membership/spacefunding, site-wide); demoted "potential chapter leads" from primary to secondary audience and reframed growth toward participation in existing chapters; resolved the Facebook-vs-site open question (site is canonical, FB stays for reach/turnout); added a light-and-broad positioning guardrail; logged the private organiser back-office + "who's coming" as an explicit OPEN question (deferred to post-interview). Scope: v1 is bilingual NL+FR (English deferred); added a Membership/spacefunding page + global footer CTA; confirmed cuts (web store, photo-gallery system, poster/flyer generator, worked-out chapter-start flow); low-frequency per-chapter subscriptions; clearer volunteer onboarding. Structure: sitemap + language routing updated, `/membership` node added, Key Structural Decisions revised, Decision Flags reconciled (4 of 5 resolved; Grande Kidical still open).

## [2026-05-18] ingest | Leticia interview — structured transcript

Ran the Leticia site-strategy interview (2026-05-18). Synthesised the raw auto-transcription into a structured, theme-organised document at `docs/raw/interview-leticia-2026-05-18.md` (governance, scale, funding, audiences, events workflow, subscriptions, scope in/out, the contested organiser back-office, ecosystem/politics, next interviews, look & feel). Key new signal: funding/membership is a top org goal absent from Strategy; "potential chapter lead" as a primary audience is overstated (challenge is participants, not groups); Facebook stays as a turnout signal; trilingual → bilingual NL/FR for v1. Evaluation against `ux-planning.md` (strategy + scope) pending user confirmations.

## [2026-05-18] ux-planning | Content Migration Plan — page-by-page table + meeting visual

Completed the previously-stubbed Content Migration Plan in `ux-planning.md`. Added the full page-by-page mapping (every current Wix page → action per the Rewrite/Migrate/Merge/Absorb/Drop/Seed legend → new home), grouped into National & content, Events, Chapters, and News/Press/Store, sourced from `site-audit.md` and the new sitemap. Added a "Decision Flags for Leticia" subsection surfacing the five non-technical choices the new structure makes (web store, photo gallery, "no bike" demotion, Grande Kidical normalisation, Liège external). Produced a derived standalone meeting artifact at `docs/site-structure-comparison.html` — a self-contained side-by-side visual (current vs new sitemap, colour-coded actions, decision flags) for the site-strategy review with Leticia; kept separate from the wiki internals. Removed the redundant `docs/raw/current-live-site-kidicalmass-be.md` (superseded by the existing `docs/raw/website/` scrape + `site-audit.md`).

## [2026-04-13] image-map | Photography expanded — Downloads batch

Added 23 new photos from `~/Downloads/kidical-photos/`. Identified 4 duplicates: upgraded `ride-brussels-boulevard-crowd.jpg` (787→1960px wide) and `volunteer-pink-vest.jpg` (740→1200px tall) with higher-res originals; skipped `cf0153_6f0e0e6d` and `cf0153_0a3acc1b` as duplicates of `kidical-mass-16` and `kidical-mass-13`. Photography collection now 36 photos. Highlights: three 8688×5792 professional shots, Cinquantenaire crowd at 5472×3648, DJ-on-boombox-bike. Homepage and About hero slots upgraded from ⚠️ to ✅. Updated `image-map.md` and `image-map-new-site.md`.

## [2026-04-13] image-map | Asset catalogue + new site slot map

Visually assessed all 143 files in `docs/raw/website/assets/`. Applied exclusion criteria from spec (size, screenshots, iStock, social story formats, Wix UI elements, QR codes, third-party event assets). Copied 50 usable assets to `docs/raw/assets/` with clean kebab-case names: 11 illustrations, 13 photos, 11 event posters, 12 chapter posters, 3 logos. Wrote `image-map.md` (asset catalogue) and `image-map-new-site.md` (per-page slot map with gap priority list).

## [2026-04-13] ingest | Notion project — initial migration

Migrated 7 pages from the Notion project "Kidical Mass Belgium — Website Project" verbatim:
desk-research, service-design, key-decisions, tone-of-voice-notion, strategy-plan, site-audit, ux-planning (with 14 nested child pages).
Excluded: Meeting Notes (top-level template only), Look & feel examples.

## [2026-04-13] ingest | Existing codebase docs — initial migration

Migrated docs/ux/ (strategy, scope, structure, activity-detail sub-pages) and docs/tone-of-voice.md into the wiki verbatim.

## [2026-04-13] ux-planning | Full page-level UX specs — all remaining pages

Completed thorough UX planning for all remaining pages, matching the depth of the Activity Detail spec. Each page now has: deepened Strategy (psychological depth, user mental states), confirmed Scope, confirmed Structure, ASCII wireframes (desktop + mobile with annotations), and an Open Questions / Necessary Refinements section.

Pages completed:
- **Home** — hero structure, events strip, chapter map, stats, volunteer CTA, news, partners bar
- **Events Overview** — filter bar, date-grouped list (upcoming + past modes), Grande KM badge
- **Getting Started** — FAQ, "don't have a bike" section (added My Kids Bikes + Cyclo note from raw site), 3 structured "other activities"
- **Help Out** — 5 role cards, "what joining looks like" honest commitment section (from raw volunteer page), routed contact form, start-a-chapter CTA
- **Chapters Overview** — map + list, Brussels clustering, Flanders hidden, start CTA
- **Chapter Page Template** — events auto-populated, team + volunteer form merged, optional sections hidden
- **About / Mission** — NEW: full 5-plane spec from scratch. Strategy, scope, structure, wireframes
- **About Overview** — deepened strategy, 6 sub-section cards, stat bar, CTA
- **About / Organisation** — organigram, 3 levels, coordination duo named, safety/routes, open questions resolved
- **About / Vision** — 4 policy demands, Child Friendly City coalition, parent quotes, open questions resolved
- **About / News** — article feed, article detail page wireframe, open questions
- **About / Press** — featured items, full list, auto-aggregation, dead link strategy
- **About / Partners** — 3 categories, 6 confirmed partners, "become a partner" CTA

## [2026-04-13] content | Activity Detail — Content template

Created `ux/activity-detail-content.md`: full copy template for the `/events/[slug]` page. Covers all 8 sections (hero, practical strip, what to expect, chapter context, team + volunteer ask, partners, photo permission, meta). EN/FR/NL for all static copy; database-driven fields marked as variables. Includes default "what to expect" body, two event-specific theme examples (Spooky Edition, Safety First campaign), and final photo permission copy.

## [2026-04-13] write | UX content pages — all 7 sections
All 7 UX wireframe pages now have companion content pages with full EN copy and FR/NL inline notes.

## [2026-04-13] restructure | UX wiki reorganisation

Restructured UX content into cleaner buckets. ux-planning.md now holds site-level content (principles, strategy, scope, sitemap, migration plan). Extracted all 14 page specs from the raw Notion dump into 7 files in ux/: activity-detail.md, events-overview.md, home.md, help-out.md, getting-started.md, chapters.md (overview + template), about.md (all about/* pages). Removed old plane files (_strategy, _scope, _structure, _skeleton) and the activity-detail subfolder.
