---
title: Skeleton — page registry & build pipeline
tags: [design, skeleton, build]
sources: [wiki/design/20-structure, wiki/design/30-skeleton]
phase: design
updated: 2026-06-03
---

# Skeleton — page registry & build pipeline

Every page/route as one row, with its **honest pipeline status**. This table is the single source of truth; the read-only **`/build`** dashboard (non-prod, unlinked) parses it live. Update procedure + honesty gate live in [`CLAUDE.md`](../../../../CLAUDE.md) → "Updating the build pipeline" (or run `/pipeline`).

**Phases:** `UX` briefing · `Conf` content-confidence (1–5) · `Wire` wireframe (content + hierarchy, no style) · `Assets` media in · `UI` visual style · `Back` backend/CMS wired & verified · `OK` client sign-off.
**Status:** 🔴 niet begonnen · 🟠 bezig · 🟢 goed · ⚪ n.v.t. · ❓ te beslissen. `OK` is binary (🔴/🟢).

> **Initial seed (2026-06-02) — provisional.** Stages below are an honest first pass, not yet verified row-by-row with Nico against the running app. The six model-backed views (Home, Events, Event detail, Chapters, Chapter page, News) are **rebuilt + rebranded** → `Wire`/`UI` 🟠 (never 🟢 until Frederik's own critique + refine pass). `Back` 🟠 = models exist, wiring unverified live. `Conf` is seeded low where client material is still pending. Correct rows as reality is confirmed.

| ID | Page | Slug | Type | UX | Conf | Wire | Assets | UI | Back | OK | Top gaps |
|---|---|---|---|---|---|---|---|---|---|---|---|
| P-01 | **Home** | `/` | Conv | 🟠 | 3 | 🟠 | 🔴 | 🟠 | 🟠 | 🔴 | `[ux]` UX brief incomplete — homepage structure/hierarchy not well-planned yet; needs dedicated UX pass before build advances. `[asset]` hero video/photo (gap); `[content]` live event strip + movement stats (PAT-4) from models. Rebuilt + rebranded — awaiting Frederik critique before Wire 🟢. |
| P-02 | **Events overview** | `/events` | Index | 🟢 | 3 | 🟠 | 🔴 | 🟠 | 🟠 | 🔴 | `[content]` replaces `/agenda`, redirect-critical (D-7); list + upcoming/past (PAT-12) from Activity model. **Build pass 2026-06-03 (nav label "Kalender"):** view now **NL + wired** — rides-only filter (`activity_type = kidicalmass`, controller), Aankomend/Voorbije toggle + Gemeente filter (GET params, shareable URLs), NL date/month group headers (`isoFormat` nl), NL empty states; verified live 200, 0 em-dashes, suite green (+5 tests). `[back]` per-region "mis geen fietstocht" email opt-in is an honest **placeholder** (light-blue "binnenkort" band) pending the `Email subscription` model. **Surface pass 2026-06-03 (`/critique`+`/distill`):** on the ride/show kit, appearance moved to `app.css` — **slim blue header band** (`.index-hero`, deliberately not a poster hero; the list is the point), **sticky branded filter** (`.kal-filter`), branded toggle/select, NL date headers. **Frederik critique pass 2026-06-03 (screenshot-driven):** location filter was unclear + competed with the date browse → now a **labelled "📍 Gemeente" filter** (secondary to the period toggle); **`<x-event-card>` redesigned** to a clean **calendar row** (time rail · title · meeting point w/ pin, appearance in `app.css`); **duplicate date removed** (cards take `:show-date="false"` under the day headers — the date lived in both); single-column agenda at a readable measure. Hero badge-overlap bug fixed (`badge-in` keyframe). **Structure pass 2026-06-03 (`/critique` "it's a calendar"):** **pagination removed** — a calendar flows, so upcoming shows the whole (short) run soonest-first in one scroll, past is a bounded recent archive (limit 24, no paging); **card headline now leads with the town** (drops the repeated "Kidical Mass " prefix; one-offs like "Grande/Spooky Edition" keep their full name); **relative landmarks** "Vandaag / Morgen / Dit weekend" on imminent day headers. Serves the two parent jobs (next-ride-near-me = top of the filtered list w/ landmark; which-weekend = scan the town column). **Location-first pass 2026-06-03 (mockups A/B/C → Frederik picked A):** the gemeente filter is now the **primary, prominent control** ("Waar fiets je?" + a **Flux searchable select**, `<livewire:ride-calendar>` w/ `#[Url]` state), options **ordered by postal code** + labelled "1030 – Schaarbeek" (searchable by zip **or** name); period demoted to a **"Bekijk voorbije ritten →"** link. Requires Flux JS → **`@fluxScripts` added to the public `site` layout** (Livewire/Alpine were already auto-injected). Playwright-verified: select opens, filters by name+zip, 0 JS errors. Wire/UI 🟠 — Frederik final 🟢 gate. |
| P-03 | **Event detail** | `/events/[slug]` | Detail | 🟢 | 3 | 🟠 | 🔴 | 🟠 | 🟠 | 🔴 | `[asset]` Komoot embed; `[content]` per-event partners (PAT-5); "I'm coming" + hosts display (PAT-18, D-1). Rebuilt + rebranded. **Frederik critique 2026-06-03:** added **yellow date subtitle** under the hero title (`.activity-hero__date`); meta-panel **"Wanneer" → "Startuur"** (time only, clock icon) so the date isn't duplicated. — awaiting critique. |
| P-04 | **Steun Kidical Mass** | `/steun-ons` | Conv | 🟢 | 2 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | **Built + normalized 2026-06-03** (`/critique`+`/normalize`): `/steun-ons` on the ride/show kit (`.activity-hero*` hero + `.activity-promises*` funds band + ask on white + yellow CTA band; blue→sky→white→yellow), `♥ Steun ons` nav CTA (replaces login → footer; mobile pinned), `Steun Kidical Mass` footer CTA, reusable `<x-support-callout>` on Home + event-detail; old `/membership` 301s here; Growfunding link-out (locale-aware), no on-site payment. Yellow support-pill consistent across nav/footer/callout/ask. `[client]` one-off path omitted pending Leticia + IBAN (D-9). Wire/UI 🟠 (render-verified, no screenshot) — Frederik critique pending; Back ⚪ (links out). |
| P-05 | **Contact (national)** | `/contact` | Utility | 🔴 | 1 | 🔴 | ⚪ | 🔴 | ❓ | 🔴 | `[strategy]` new national front door → coordination duo; press/partnership/general buckets. Route stubbed; no brief yet. |
| P-06 | **Legal / GDPR** | `/privacy` | Utility | 🔴 | 1 | 🔴 | ⚪ | 🔴 | ❓ | 🔴 | `[research]` legal text pending (enquiry form + email opt-in). **Folded to one page 2026-06-02** — privacy + cookies are sections of `/privacy`; `/cookies` 301s here. View still a stub. |
| P-07 | **Volunteer login** | `/login` | Utility | 🔴 | 1 | 🔴 | ⚪ | 🔴 | 🟠 | 🔴 | `[client]` login-only — no public register (invite-only). Fortify route + backend exist; no branded view. |
| P-08 | **My activities** | `/my-activities` | App | 🟠 | 2 | 🔴 | ⚪ | 🔴 | ❓ | 🔴 | `[strategy]` volunteers-only post-login landing; depends on D-1/D-2 (attendance). Provisional spec; not routed. |
| P-09 | **Chapter back-office** | `/backstage/[postal-code]` | App | 🔴 | 1 | 🔴 | 🔴 | 🔴 | ❓ | 🔴 | `[client]` separate branded frontend (not Filament, decided); per-chapter materials + attendance. Brief pending D-1/Alexandre. |
| P-10 | **Chapters overview** | `/chapters` | Index | 🟢 | 3 | 🟠 | 🔴 | 🟠 | 🟠 | 🔴 | `[content]` Belgium map + list (PAT-8); "start a chapter" CTA. **Build pass 2026-06-03 (nav label "Lokale groepen"** — renamed from "Afdelingen", Frederik-approved): card-grid drift **reverted** → NL **region-grouped name links** (Brussel→Wallonië→Vlaanderen via parent group; no count badges, no "Part of:"), real "Begin een lokale groep" CTA → Help out; verified live 200 (all 3 region headers render), 0 em-dashes, suite green (GroupsTest updated +2 tests). `[back]` Belgium **map** is an honest placeholder pending Leaflet + coords. **Surface pass 2026-06-03 (`/critique`+`/distill`+`/arrange`):** ride/show kit, appearance in `app.css` — **slim blue header band** (`.index-hero`), **list-first** (region directory leads; map demoted to a slim "binnenkort" note), region pills, **calm contained** "begin een groep" CTA (not a loud full-bleed yellow band — recruiting is the quiet beat). Wire/UI 🟠 — Frederik critique + screenshot pending. |
| P-11 | **Chapter page** | `/chapters/[postal-code]` | Template | 🟢 | 3 | 🟠 | 🟠 | 🟠 | 🟠 | 🔴 | `[content]` self-published, hide-if-empty sections (PAT-11); redirect-critical. **Re-planned as a local "home" + built NL on the ride/show kit 2026-06-03** (off [`chapters.md` § Chapter Page](chapters.md)): warm arc — identity header + shared **ride-photo band** + **next-ride hero** + **team faces** (initials avatars, names only; no role/photo field yet) + **J2 form** (built, reused) + region-fallback news (hide-if-empty) + **faux** vrienden/downloads preview + **faked** "mis geen rit" opt-in (Alpine, non-persisting). Count badges / "Part of:" / "More" dump gone. Parent-region rides aggregate in, so Brussels groups always look alive; truly-isolated groups (e.g. Luik) get the designed **"nog geen rit" empty state**. Render-verified desktop+mobile (filled + empty), 0 em-dashes (rendered), Pint clean, suite **136 green** (GroupsTest updated). **Wire 🟠 → 🟢 gated on Frederik's own critique.** `[back]` spec for Nico — per-group `intro`/cover/role/lead-email ([#37](https://github.com/ndeblauw/kidicalmass/issues/37)) + single-group `Email subscription`; press stays hide-if-empty (D-11, never faked). |
| P-12 | **Getting Started** | `/getting-started` | Info | 🟢 | 4 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | `[content]` NL wireframe built + **distilled** 2026-06-02 (Frederik critique): CTA lifted after FAQ; "Andere manieren" removed; **"Geen fiets?" folded to a FAQ link → Find a bike (P-22)**; safety FAQ + marshal card added from the volunteer ROI/Jorge interview. **Surface pass 2026-06-02 + scroll/adapt pass 2026-06-04 (Frederik-guided):** hero reuses `x-page-hero` (kid-on-bike illus.); panel background light-blue so rounded seam flows into the sky band; **"wat je mag verwachten"** rebuilt as scroll-driven sticky section — left column (heading + lead para + scooter illus.) stays fixed while 6 cards fly in one by one and stack; card typography scaled up (clamp), icon chips colour-rotated (red/cobalt/orange/ink/violet/coral); accordion FAQ + full-bleed yellow CTA unchanged. Awaiting Frederik critique before Wire/UI 🟢. |
| P-13 | **Help out** | `/help-out` | Conv | 🟢 | 3 | 🟢 | 🟢 | 🟢 | ⚪ | 🔴 | **Wire/UI 🟢 — Frederik-approved 2026-06-03.** NL orientation page on the ride/show kit: `.activity-hero*` (blue, real volunteer photo, daisy, "Doe mee" badge) + `.activity-promises*` role cards (yellow band, white tilted cards, red Flux-icon chips), real-photo joy band, group-picker climax (light-blue band), quiet coda. J2 reframe intact (form on P-11; CTA = group picker → `?intent=volunteer#aanmelden`). Photos in `public/img/volunteers/`. `OK` (client sign-off) pending. |
| P-14 | **About overview** | `/about` | Hub | 🟢 | 3 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | **Built 2026-06-03 (NL, ride/show kit):** orientation hero + 6 sub-page nav cards (red Flux-icon chips, hover-slide arrow) + mini stat bar (light-blue) + shared closing CTA. Render-verified desktop+mobile (Playwright), 0 em-dashes, suite green. `[content]` stat bar duplicates Missie's stats — single source + refresh pending duo. Journey + JTBD: [`about-journey.md`](about-journey.md). Wire/UI 🟠 — Frederik critique pending. |
| P-15 | **Mission** | `/about/mission` | Story | 🟢 | 3 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | **Built 2026-06-03 (NL):** movement intro · 3 axes (Start/Support/Spread, promises-card idiom on sky) · impact stats · inclusivity → Getting Started · Julienne pull-quote · CTA. Render-verified desktop+mobile, 0 em-dashes, suite green. `[content]` stats are **static + 2024-stale** (live PAT-4 stats deferred → Back ⚪ for now); Julienne NL translation pending duo. Wire/UI 🟠 — Frederik critique pending. |
| P-16 | **Vision** | `/about/vision` | Story | 🟢 | 3 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | **Built 2026-06-03 (NL):** position statement · 4 numbered demands (light-blue band) · manifesto link · 2 parent voices · CTA. Merges legacy /nos-revendications + /what-we-want. Render-verified, 0 em-dashes, suite green. `[asset]` manifesto PDF still points at the **legacy Wix URL** — re-host (couples D-7); parent-quote NL translations pending duo. Wire/UI 🟠 — Frederik critique pending. |
| P-17 | **Organisation** | `/about/organisation` | Info | 🟢 | 3 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | **Built 2026-06-03 (NL):** intro · semantic 3-tier organigram · national-vs-local two columns (ho-deal idiom, sky) · named coördinatieduo (Leticia & Cecilia) · safety → Getting Started · chapter-CTA. Render-verified, 0 em-dashes, suite green. `[content]` duo photos + per-person bios pending; legacy SVG organigram replaced by a CSS one (swap if wanted). Wire/UI 🟠 — Frederik critique pending. |
| P-18 | **News** | `/about/news` | Index | 🟢 | 2 | 🟠 | 🔴 | 🟠 | 🟠 | 🔴 | `[content]` Article model exists; feed + preview (PAT-17). **NL surface pass 2026-06-03** — header + lead + honest empty state (IG/FB); feed renders seeded articles. Render-verified, suite green. `[asset]` optional cover-image CMS field pending. Wire/UI 🟠 — Frederik critique pending. |
| P-19 | **Press** | `/about/press` | Info | 🟢 | 2 | 🟠 | ⚪ | 🟠 | ⚪ | 🔴 | **Built 2026-06-03 (NL), contact-forward + honest:** hero · "journalisten, we praten graag" + offer list + perscontact card (bike@kidicalmass.be) · truthful "eerder verschenen in" outlet strip (RTBF/BX1/BRUZZ/La DH/HLN/Het Nieuwsblad, **no fabricated links**) · empty-state · → Missie. Render-verified, 0 em-dashes, suite green. **`[data]` no Press model yet → no linked archive (OPEN `D-11`)**; confirm press address. Wire/UI 🟠 — Frederik critique pending. |
| P-20 | **Partners** | `/about/partners` | Info | 🟢 | 3 | 🟠 | ⚪ | 🟠 | 🟠 | 🔴 | **Built 2026-06-03 (NL), curated static:** hero · institutionele partners en bondgenoten (Brussel Mobiliteit, Brussel Stad, Schaarbeek, Clean Cities) · operationele/in-kind (Loopz, Kidical Mouse, My Kids Bikes) → Find a bike · "partner worden" mailto CTA. Render-verified, 0 em-dashes, suite green. **`[data]` `partners` table now holds 15 real seeded records + NL descriptions (`d9343c4`), but still no cleared logos, no category field → page stays static, not model-bound (OPEN `D-11`)**; all-Brussels list needs a national pass (Leticia). Brief: [`partners.md`](partners.md). Wire/UI 🟠 — Frederik critique pending. |
| P-21 | **Admin (Filament)** | `/admin` | Admin | ⚪ | — | ⚪ | ⚪ | ⚪ | 🟢 | ⚪ | Filament panel — coordination duo = all, chapter lead = own. Runs atop existing models. Exempt from the public pipeline. |
| P-22 | **Find a bike** | `/find-a-bike` | Info | 🟢 | 4 | 🟠 | ⚪ | 🔴 | ⚪ | 🔴 | `[content]` no-bike providers lifted off Getting Started 2026-06-02 (Kidical Mouse, Loopz, Fietsbieb, My Kids Bikes, Cyclo); facts verified. Standalone resource, reached from the Getting Started FAQ — **not in main nav**; reusable from event/chapter pages. `[client]` Kidical Mouse availability open. Awaiting UI pass. |

## Roll-up

- **Pages:** 22 rows (`P-01`–`P-22`). **UX:** 16 🟢 + 2 🟠 (Home — UX brief incomplete; My activities) + 4 🔴 (stubs: Contact, Legal, Login, Back-office). **Avg content-confidence ≈ 2.3 / 5** — client material still pending across most pages.
- **Wire/UI 🟢 (Frederik-approved):** **Help out** — the first page fully through the design pass (ride/show-kit re-skin + real volunteer photography + Frederik's own critique). Only `OK` (client sign-off) remains.
- **Built + rebranded (Wire/UI 🟠):** Home, Events, Event detail, Chapters, News — five of the six model-backed views. **Wire 🟢 is gated on Frederik's critique** — these still await theirs (Help out is the first page through it).
- **Re-planned + rebuilt as a local "home" (2026-06-03):** **Chapter page (P-11)** — Frederik flagged the old build as not a "home" (EN, off-kit, flat stack of metadata). Fresh Strategy→Skeleton re-plan + Impeccable critique ([`chapters.md` § Chapter Page](chapters.md)), then **built NL on the ride/show kit**: warm arc (identity → ride-photo band → next-ride hero → team faces → region-fallback news → faux extras → faked opt-in), count badges / "Part of:" / "More" dump gone, designed empty state for isolated groups. **UX 🟢, UI/Assets/Back 🟠; Wire 🟠 → 🟢 gated on Frederik's own critique.** Backend spec for Nico (per-group intro/cover/role/lead-email + single-group subscription).
- **Surface pass done (Wire 🟠, UI 🟠):** **Getting Started** — built on the ride/show kit (`.activity-hero*` / `.activity-promises*`, full-bleed colour bands, white tilted cards, red Flux-icon chips); and **Steun Kidical Mass** (P-04, built 2026-06-03 — blue hero, funds cards with red chips, ask card + light-blue free-reassurance, yellow movement-scale band; render-verified, no screenshot). Wire/UI 🟢 still gated on Frederik's critique.
- **Built at wireframe fidelity (Wire 🟠, UI 🔴):** Find a bike (the no-bike resource offshoot) — NL structural view, content verified. Surface/UI pass still pending.
- **Back:** 🟠 (in progress, unverified) on the six model-backed pages + Login (Fortify) + News + Partners (Partner model exists but the page is static, not bound — `D-11`); ❓ where need/shape is open (Contact, Legal, My activities, Back-office); ⚪ on the static About leaves (Hub, Mission — live PAT-4 stats deferred — Vision, Organisation, Press) + Getting Started + Find a bike + Help out (now static — its routed form lives on the chapter page) + **Steun Kidical Mass** (links out to Growfunding). **OPEN `D-11`:** Partners + Press are static-shipped and need real data (partner records **seeded `d9343c4`**; still logos + category + table-binding; a Press model) to advance.
- **About section built (Wire 🟠, UI 🟠) — 2026-06-03:** the hub (P-14) + Missie/Visie/Organisatie/Pers/Partners (P-15/16/17/19/20) built in NL on the ride/show kit (`.activity-hero*`, full-bleed colour bands, white tilted cards, red Flux-icon chips, shared `<x-about-cta>` + `<x-about-reveal>`), News (P-18) NL-passed; screenshot-verified desktop + mobile, 0 em-dashes, suite green (+8 About tests). New `about-*` CSS namespace in `app.css`. Journey + JTBD + open questions: [`about-journey.md`](about-journey.md). **Wire/UI 🟢 gated on Frederik's critique. Partners + Press `Back` blocked on OPEN `D-11`** (no real partner/press data — static-shipped honestly).
- **Routed but not built:** Contact + Legal views are routed (`routes/web.php`, `{locale}` prefix) but still stubs — rows stay Wire 🔴. (Getting Started, Steun Kidical Mass, and the **whole About section** are now built.) Legal is now **one page** — `/cookies` 301s to `/privacy` (folded 2026-06-02).
- **Cross-cutting — OPEN concern (`D-10`, flagged 2026-06-03):** **page metadata / SEO / social share previews have never been addressed.** The public `<head>` ([`layouts/site.blade.php`](../../../../resources/views/layouts/site.blade.php)) is a stub — titles are bare/unsuffixed (Help-out is just "Meehelpen"), the meta description is hardcoded English site-wide, and there are **no Open Graph / Twitter Card / canonical / `hreflang` tags**. Shared links render with no preview. Affects **every row**; not a per-P gap. Needs a head-metadata partial (build) + a title pattern, per-page descriptions in voice, and an OG-image-per-page-type plan (design). See [`01-concerns.md` § D-10](../01-concerns.md). Do not call the site launch-ready until resolved.
- **OPEN concern (`D-11`, flagged 2026-06-03):** the two About credibility leaves — **Partners (P-20)** and **Press (P-19)** — have **no real data** behind them (the `partners` table is faker rows with no logos/category; there is no Press model). Both shipped **honestly static**; to advance `Back` they need real partner records + cleared logos + a category field, and a `Press` model with a verified item list. **Update 2026-06-03 (`d9343c4`):** the seed now creates **15 real partner records + NL descriptions** (local DB only), so the *records* half is in hand; still outstanding are **cleared logos, a `category` field, and binding the page + partner-strip to the table**. This is also **launch-visible site-wide**: the slim `partner-strip` still finds no bound national partner records and renders only the hardcoded Brussel Mobiliteit logo. See [`01-concerns.md` § D-11](../01-concerns.md).
- **Global shell (not a P-row):** the public **footer + partner strip** are NL-localised via `lang/nl/footer.php` + `lang/nl/partners.php`. **Partner strip reworked 2026-06-03:** the full-bleed "Partners & sponsors" band (logos + acquisition + "Ook ondersteund door" + illustration) was reduced to a **slim recognition strip** ([PAT-5](../40-patterns.md)) — "Mede mogelijk gemaakt door" + muted logos + one link → `/about/partners`; acquisition + supporters list + categories moved to **P-20**. Brief: [`partners.md`](partners.md). The **fat-footer menu mirrors the IA** — *Ontdek* (main nav), *Over ons* (About sub-nav), persistent **Steun Kidical Mass** CTA → `/steun-ons`, and a utilities bottom bar (Contact · Privacy & cookies · Inloggen). Social = Instagram + Facebook (IG `kidicalmass.belgium`, FB `Kidicalmass.brussels`). **Support rework built 2026-06-03:** the header carries a **`♥ Steun ons`** accent CTA (replaces login → login is footer-only; mobile = pinned first in the hamburger menu); "steun" replaces "lid" sitewide (`lang/nl/support.php`); reusable `<x-support-callout>` on Home + event-detail. See P-04 + [`10-scope.md` § Support](../10-scope.md).
- **Proposed build order** (from migration): Events → Chapters → Help out → Getting Started → About → Home. Validate with Nico ([25-content-migration.md](../25-content-migration.md)).
